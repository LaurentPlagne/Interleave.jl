var documenterSearchIndex = {"docs":
[{"category":"section","location":"applications/reductions/#Per-instance-reductions","page":"Per-instance reductions","text":"The common GPU benchmark suite also measures squared norms and dot products. They are useful here as a control case: the reduction is sequential inside one instance, while independent instances remain available for SIMD or GPU work.\n\nfunction batch_squarednorm!(out, A)\n    acc = zero(eltype(A))\n    @inbounds for i in eachindex(A)\n        x = A[i]\n        acc += x * x\n    end\n    out[1] = acc\n    out\nend\n\nThe same function is passed to all three drivers:\n\napply!(batch_squarednorm!, out, A)\nparallel_apply!(batch_squarednorm!, out, A)\ngpu_apply!(batch_squarednorm!, d_out, d_A; wait = true)\n\nOnly the container and execution backend change. There is no reduction-specific GPU shader in Interleave.jl. Each GPU work item owns one vector and writes one result, so the batch axis is still coalesced. The reduction is intentionally included as a control workload: for a large contiguous scalar vector, ordinary compiler SIMD may already be excellent, and a large packet size is not automatically beneficial.\n\nThe benchmark is part of the common suite:\n\njulia --project=bench -t auto bench/reductions.jl\n\nThe Metal, CUDA, and AMDGPU workflows run the same two kernels through KernelAbstractions when the corresponding runners are configured. Their result is a source-reuse and correctness check across backends, not a claim that one work item per reduction is the best possible vendor-specific reduction implementation.","title":"Per-instance reductions"},{"category":"section","location":"tutorials/adi/#Tutorial-3-—-Repacking-a-3-D-ADI-solver","page":"3 · 3-D ADI sweeps","text":"Alternating-direction implicit schemes solve many independent tridiagonal lines along one grid direction, then another. The recurrence direction changes, but the line solver does not have to.\n\n(Image: Changing recurrence and packed axes during ADI sweeps)","title":"Tutorial 3 — Repacking a 3-D ADI solver"},{"category":"section","location":"tutorials/adi/#1.-Separate-physical-axes-from-kernel-axes","page":"3 · 3-D ADI sweeps","text":"Let the physical grid be (nx, ny, nz). During an x-sweep:\n\neach recurrence runs along x;\ndifferent y or z lines are independent;\none independent grid axis can become the Interleave batch dimension.\n\nFor example, packing across y gives a logical batch of shape (ny, nx, nz):\n\nusing Interleave\n\nnx, ny, nz = 32, 20, 12\nX = Interleave.Array{Float32,3,8}(undef, ny, nx, nz)\n(size(X), instance_size(X), size(parent(X)))\n((20, 32, 12), (32, 12), (32, 12, 3))\n\nThe logical indices are X[y, x, z]. A kernel instance has shape (nx, nz) and elements of type Vec{8,Float32}. It can solve every x-line in that instance with ordinary (x,z) indices.","title":"1. Separate physical axes from kernel axes"},{"category":"section","location":"tutorials/adi/#2.-Write-a-line-solver,-not-an-axis-specific-solver","page":"3 · 3-D ADI sweeps","text":"function thomas_lines!(x, d, u, l, b, scratch)\n    n, nlines = size(x)\n    @inbounds for line in 1:nlines\n        pivot = d[1, line]\n        invpivot = inv(pivot)\n        x[1, line] = b[1, line] * invpivot\n        for i in 2:n\n            scratch[i] = u[i - 1, line] * invpivot\n            pivot = d[i, line] - l[i, line] * scratch[i]\n            x[i, line] = b[i, line] - l[i, line] * x[i - 1, line]\n            invpivot = inv(pivot)\n            x[i, line] *= invpivot\n        end\n        for i in n-1:-1:1\n            x[i, line] -= scratch[i + 1] * x[i + 1, line]\n        end\n    end\n    x\nend\nthomas_lines! (generic function with 1 method)\n\nThe solver only knows that its first local dimension is the recurrence direction. Layout decides whether that dimension represents physical x, y, or z.","title":"2. Write a line solver, not an axis-specific solver"},{"category":"section","location":"tutorials/adi/#3.-Prepare-one-sweep","page":"3 · 3-D ADI sweeps","text":"X, D, U, L, B = (Interleave.Array{Float32,3,8}(undef, ny, nx, nz) for _ in 1:5)\nfill!(X, 0); fill!(D, 2); fill!(U, -1); fill!(L, -1); fill!(B, 1)\n\napply!(thomas_lines!, X, D, U, L, B;\n       scratch=Vector{packtype(X)}(undef, nx))\nsize(X)\n(20, 32, 12)\n\nThe scratch buffer has one entry per recurrence position and uses the packed element type. For parallel execution, pass the same object as a prototype; the driver creates one copy per chunk.","title":"3. Prepare one sweep"},{"category":"section","location":"tutorials/adi/#4.-Change-direction-by-changing-layout","page":"3 · 3-D ADI sweeps","text":"For a y-sweep, construct or transpose into a layout whose instance starts with ny. One possible logical ordering is (nx, ny, nz): x supplies lanes, while each kernel instance has shape (ny, nz).\n\nY = Interleave.Array{Float32,3,8}(undef, nx, ny, nz)\n\nProduction ADI codes commonly transpose between directions for locality anyway. Interleave makes the purpose explicit: the first logical dimension is the current population of independent lines, and the first instance dimension is the recurrence direction.\n\ntip: Choose the cheapest independent axis\nIf both remaining grid axes are independent, pack the one that minimizes transposition cost and produces enough full packets. The packed axis does not have to be the largest, but very small batches waste lanes.","title":"4. Change direction by changing layout"},{"category":"section","location":"tutorials/adi/#5.-When-repacking-is-worth-it","page":"3 · 3-D ADI sweeps","text":"Repacking is paid once; the recurrence may run for many time steps or nonlinear iterations. The approach is compelling when the packed layout is reused enough times to amortize data movement. It is weak when every solve is tiny and a full transpose is required before and after each call.\n\nMeasure the complete stage:\n\nreorder → repeated line solves → reorder back\n\nDo not report the line solver alone if the application cannot keep data in the packed layout.","title":"5. When repacking is worth it"},{"category":"section","location":"manual/parallel/#Explicit-task-parallelism","page":"Task parallelism","text":"SIMD and task parallelism are separate decisions.\n\napply!(kernel!, output, input)  # sequential packet traversal\n\nparallel_apply!(kernel!, output, input;\n                scheduler=StaticScheduler())\n\napply! cannot launch tasks: it has no scheduler or chunk-count keyword. This makes it safe inside a caller that already owns parallelism. parallel_apply! makes task creation visible at the call site.","title":"Explicit task parallelism"},{"category":"section","location":"manual/parallel/#Choose-a-scheduler","page":"Task parallelism","text":"The exported OhMyThreads schedulers cover common policies:\n\nStaticScheduler() for uniform packet cost;\nDynamicScheduler() or GreedyScheduler() for uneven work;\nSerialScheduler() to exercise the parallel driver without concurrent tasks.\n\nThe default number of chunks is 4 * Threads.nthreads(). Override nchunks only after measuring; too many chunks increase launch overhead, while too few reduce load balance.","title":"Choose a scheduler"},{"category":"section","location":"manual/parallel/#Scratch-storage-is-per-chunk","page":"Task parallelism","text":"When a prototype array is supplied, the driver calls similar once per chunk:\n\nparallel_apply!(kernel!, output, input;\n                scratch=scratchlike(output),\n                scheduler=StaticScheduler())\n\nNever return the same mutable scratch object to every task. That creates a race even when each packet writes to independent output.","title":"Scratch storage is per chunk"},{"category":"section","location":"manual/parallel/#Expect-bandwidth-ceilings","page":"Task parallelism","text":"Threads multiply throughput only while cores have independent resources available. A compute-heavy recurrence that reuses cached state may scale well. A solver streaming five large arrays can saturate memory bandwidth after only a few cores.\n\nMeasure the best sequential P first, then add tasks. Reporting SIMD speedup multiplied by thread count is not a substitute for an end-to-end measurement.","title":"Expect bandwidth ceilings"},{"category":"section","location":"manual/parallel/#Test-determinism","page":"Task parallelism","text":"Run the parallel result several times and compare each run exactly with the sequential result. Reproducibility alone is insufficient—a deterministic indexing error can repeat. Agreement with the scalar oracle, agreement with sequential DLI, and repeated parallel agreement test different failure modes.","title":"Test determinism"},{"category":"section","location":"applications/biquad/#IIR-biquad-filter-bank","page":"IIR biquad filter bank","text":"A direct-form-I biquad has four pieces of history. For each channel,\n\ny_n=b_0x_n+b_1x_n-1+b_2x_n-2-a_1y_n-1-a_2y_n-2\n\nThe feedback terms make time sequential. A bank of independent channels, however, has exactly the population parallelism Interleave needs.\n\n(Image: Four feedback filters advancing in SIMD lockstep)","title":"IIR biquad filter bank"},{"category":"section","location":"applications/biquad/#Hear-it","page":"IIR biquad filter bank","text":"<!-- ATTENTION au chemin : Documenter NE RÉÉCRIT PAS les liens dans un bloc `@raw html`, et\n     `prettyurls` (actif en CI, donc sur le site publié) rend cette page à\n     applications/<nom>/index.html, soit un niveau plus profond qu'en local. Le `../../` ci-\n     dessous est donc correct EN PRODUCTION et cassé dans un `make.jl` lancé sans CI=true.\n     Pour prévisualiser les médias en local : CI=true julia --project=docs docs/make.jl -->\n<div class=\"interleave-media\">\n  <audio controls preload=\"none\" style=\"width: 100%;\">\n    <source src=\"../../assets/media/biquad_demo.mp3\" type=\"audio/mpeg\">\n    Your browser does not support the audio element.\n  </audio>\n  <p class=\"interleave-caption\">\n    0–3 s: a raw sawtooth Am7 chord. 3–6 s: the same chord through a 600 Hz low-pass biquad.\n  </p>\n</div>\n\nThis is not an illustration of the algorithm — it is the algorithm. The 64 partials of the chord are the 64 channels of the batch, they go through apply!(biquad!, …) together, and what you hear is their sum. The generator asserts that the packed result is bit-exact against the scalar reference before it writes a single sample, so a demo that sounded right while the kernel was wrong could not be produced.\n\nRegenerate it with:\n\njulia --project=docs docs/media/make_biquad_demo.jl\nffmpeg -f s16le -ar 44100 -ac 1 -i docs/media/biquad_demo.pcm -b:a 96k out.mp3\n\nThe script prints the measured band response — -0.0 dB at 200 Hz, -2.8 dB at the 600 Hz cutoff, -15.9 dB at 1500 Hz, -29.3 dB at 3 kHz — which is the textbook second-order Butterworth curve.\n\nThe moving packet in the animation is a sample index, not a time window: every lane owns the complete state of one channel, so channels never contaminate each other.","title":"Hear it"},{"category":"section","location":"applications/biquad/#The-benchmark","page":"IIR biquad filter bank","text":"bench/biquad.jl processes 4,096 channels of 1,024 samples with fixed Float32 coefficients. The estimate is five multiplies and four additions per sample.\n\nconfiguration time estimated GFlop/s speedup vs scalar LLVM SIMD\nscalar reference 12.95 ms 2.9 1.00× —\nP=1 12.82 ms 2.9 1.01× —\nP=2 6.43 ms 5.9 2.01× yes\nP=4 3.18 ms 11.9 4.07× yes\nP=8 1.61 ms 23.4 8.03× yes\nP=16 0.85 ms 44.3 15.19× yes\nP=32 1.07 ms 35.1 12.05× yes\n\nThe nearly proportional scaling through P=16 is the signature of a latency-bound recurrence. P=32 then regresses: wider is a candidate to measure, not a monotonic promise. P=1 also validates that the driver and views do not impose a material cost on the scalar path.","title":"The benchmark"},{"category":"section","location":"applications/biquad/#With-explicit-task-parallelism","page":"IIR biquad filter bank","text":"configuration time estimated GFlop/s speedup vs scalar speedup vs threaded P=1\nP=1, 10 threads 2.02 ms 18.7 6.41× 1.00×\nP=2, 10 threads 1.13 ms 33.4 11.46× 1.79×\nP=4, 10 threads 0.61 ms 61.6 21.14× 3.30×\nP=8, 10 threads 0.41 ms 91.8 31.49× 4.91×\nP=16, 10 threads 0.34 ms 110.8 38.00× 5.93×\nP=32, 10 threads 0.55 ms 69.2 23.72× 3.70×\n\nThe plateau from P=8 to P=16 after threading says that another resource—memory traffic, core scheduling, or instruction throughput—has replaced the original recurrence bottleneck.","title":"With explicit task parallelism"},{"category":"section","location":"applications/biquad/#Competing-approaches","page":"IIR biquad filter bank","text":"DSP.jl should be the first choice when the problem is standard filter design and application. It provides biquad and second-order section representations, stateful filters, in-place APIs, and column-wise filtering.\nOrdinary task parallelism across channels is simple and complementary. It is often enough for a small number of long channels; Interleave adds fine-grained SIMD when the channel bank is wide enough.\nHand-written explicit SIMD can put one channel in each vector lane, but the state variables, loads, stores, and tail handling become packet-specific source code. Interleave obtains the same mapping by changing the element type seen by the existing recurrence.\nA GPU kernel can map channels to threads when signals are already device-resident and the batch amortises launch and transfer overhead. Interleave targets the lower-friction CPU case.\n\nInterleave is not a filter-design library and does not compete with DSP.jl's catalogue. Its claim is narrower: a custom feedback kernel can become cross-channel SIMD without being rewritten as an explicit vector program.\n\nFor the step-by-step version, see Tutorial 2.","title":"Competing approaches"},{"category":"section","location":"manual/usage/#Working-with-a-batch","page":"Working with a batch","text":"","title":"Working with a batch"},{"category":"section","location":"manual/usage/#Construct-it","page":"Working with a batch","text":"Interleave.Array follows the parameter order of Base.Array and adds packet size P:\n\nusing Interleave\n\nA = Interleave.Array{Float32,2,8}(undef, 61, 32)\n(size(A), eltype(A), packsize(A), npacks(A), npadding(A))\n((61, 32), Float32, 8, 8, 3)\n\nThe first dimension is always the batch. Every later dimension belongs to one logical problem. Packet size must be a power of two.\n\nAn existing scalar array—including the result of a comprehension—can be packed directly:\n\nC = Interleave.Array{Float32,2,8}([\n    Float32(job) + Float32(i) / 10 for job in 1:13, i in 1:6\n])\n(size(C), packsize(C), C[3, 2])\n((13, 6), 8, 3.2f0)\n\nThis convenient constructor materializes the scalar source and then copies it into packed storage. Prefer undef plus broadcast or an initialization loop when that temporary would matter.\n\nWhen P is a runtime choice, use the keyword constructor:\n\nP = 4\nB = Interleave.Array{Float32}(undef, 17, 6, 5; pack=Val(P))\n(size(B), instance_size(B))\n((17, 6, 5), (6, 5))","title":"Construct it"},{"category":"section","location":"manual/usage/#Fill-and-inspect-it-as-scalars","page":"Working with a batch","text":"A Interleave batch is an AbstractArray{T}. Normal scalar indexing, broadcasting, reductions, views, copying, and collection operate on the logical shape:\n\nfill!(A, 0)\nfor job in axes(A, 1), i in axes(A, 2)\n    A[job, i] = job + i / 100\nend\n\n(A[3, 7], sum(@view A[1:2, :]), Array(A) isa Matrix{Float32})\n(3.07f0, 106.56f0, true)\n\nPadding is not part of this logical view and cannot leak into sum(A) or Array(A).","title":"Fill and inspect it as scalars"},{"category":"section","location":"manual/usage/#Give-packed-instances-to-a-kernel","page":"Working with a batch","text":"packet(A, k) exposes the kth packet to the hot kernel:\n\nv = packet(A, 1)\n(size(v), strides(v), eltype(v))\n((32,), (1,), Vec{8, Float32})\n\nwarning: `packet` and `instance` are not the same thing\nk in packet(A, k) is a packet index running 1:npacks(A); b in instance(A, b) is a problem index running 1:size(A, 1). One packet holds packsize problems, so on an interleaved array the two ranges differ by a factor of P even though the views have the same shape.B = Interleave.Array{Float32,2,8}(undef, 100, 16)\n(npacks(B), eltype(packet(B, 1)), eltype(instance(B, 1)))\n(13, Vec{8, Float32}, Float32)A kernel is always called on packets. instance is the honest one-problem view: use it for debugging, comparison against a scalar reference, and I/O — it reads one lane out of each packet and is not a hot path. On a plain Base.Array the two coincide, because P == 1, which is exactly what makes the scalar development path interchangeable with the packed one.\n\nMost callers should use apply! rather than loop over packets themselves:\n\napply!(kernel!, output, input)\n\nFor composition with Julia's standard iteration vocabulary, packs returns a lazy vector of instance views:\n\nforeach(kernel!, packs(output), packs(input))\n\nBoth forms are sequential.","title":"Give packed instances to a kernel"},{"category":"section","location":"manual/usage/#Start-with-a-standard-array","page":"Working with a batch","text":"A Base.Array is a valid batch with packet size one:\n\nS = zeros(Float32, 12, 20)\n(packsize(S), npacks(S), instance_size(S), eltype(packet(S, 1)))\n(1, 12, (20,), Float32)\n\nThis is the recommended development path: make the kernel correct with ordinary arrays, then change the type. Because Julia is column-major, packet(S, 1) is strided when the batch is first, whereas a Interleave packed instance is contiguous. The standard path is an oracle and a P=1 option, not necessarily the fastest scalar storage for every application.","title":"Start with a standard array"},{"category":"section","location":"manual/usage/#Understand-logical-and-physical-shapes","page":"Working with a batch","text":"For A = Interleave.Array{Float32,3,8}(undef, 61, 6, 5):\n\nexpression result meaning\nsize(A) (61, 6, 5) scalar user view\neltype(A) Float32 scalar user element\nsize(parent(A)) (6, 5, 8) packed physical storage\neltype(parent(A)) Vec{8,Float32} kernel element\nsize(packet(A, 1)) (6, 5) one packed problem view\n\nparent(A) is intentionally public for low-level inspection and integration. Kernels normally receive its views from the driver.","title":"Understand logical and physical shapes"},{"category":"section","location":"manual/usage/#Empty-and-partial-batches","page":"Working with a batch","text":"An empty batch is valid. apply!, parallel_apply!, packs, and foreach perform no kernel calls. A non-multiple batch allocates one partial final packet; its unused lanes are initialized to zero and hidden from the logical array.\n\nThe padding lanes still execute the kernel. Avoid kernels for which even a zero-initialized dummy lane triggers an invalid operation. If validity depends on per-job data, initialize all kernel inputs so the padded computation stays in-domain.","title":"Empty and partial batches"},{"category":"section","location":"manual/usage/#Wrap-existing-packed-storage","page":"Working with a batch","text":"If data already has the native physical representation, it can be wrapped without copying:\n\nstorage = fill(Vec{4,Float32}(0), 32, 3)\nC = Interleave.Array(storage, 10)\n(size(C), parent(C) === storage, npadding(C))\n((10, 32), true, 2)\n\nThe constructor clears unused lanes in the last packet to preserve the padding invariant.","title":"Wrap existing packed storage"},{"category":"section","location":"manual/usage/#Workspace","page":"Working with a batch","text":"A kernel must never allocate. When it needs scratch storage, the driver owns it and passes it as the last argument. scratchlike builds the right prototype:\n\napply!(thomas!, X, D, U, L, B; scratch = scratchlike(X))\n\nparallel_apply! copies that prototype once per chunk, so every task owns its workspace; sharing one buffer between tasks would be a race.\n\ndanger: CPU and GPU workspaces are different shapes\napply! wants one instance of workspace — scratchlike(A). gpu_apply! wants a batch-major device array, one private row per work item — gpu_scratchlike(A). They are not interchangeable. Passing a batch-major buffer to apply! used to work by accident through linear indexing while computing on the wrong memory; both drivers now reject the wrong shape with an explicit message.","title":"Workspace"},{"category":"section","location":"manual/usage/#Choosing-P","page":"Working with a batch","text":"P is a layout parameter, not the hardware vector width, and the best value differs by kernel and by machine. tune measures it instead of guessing:\n\nresult = tune(thomas!, P -> setup(65_536, 64, P))\nresult.best\n\nTuningResult for thomas! — best P = 32\nP     time          vs P=1\n--------------------------------\n1     29.862 ms     1.0x\n2     14.934 ms     2.0x\n4     7.44 ms       4.01x\n8     4.171 ms      7.16x\n16    2.208 ms      13.52x\n32    1.631 ms      18.31x    <-\n\nOn this machine P = 32 is four times the 128-bit NEON width and still wins: a recurrence is latency-bound, and several vectors in flight hide the dependency chain. That is why the value has to be measured. See What you would write instead for the cases where the answer is P = 1.","title":"Choosing P"},{"category":"section","location":"tutorials/thomas/#Tutorial-1-—-Vectorizing-a-tridiagonal-solve","page":"1 · Tridiagonal recurrence","text":"This tutorial starts with an algorithm a compiler cannot normally vectorize: the Thomas solver for a tridiagonal linear system. By the end, the same Julia function will solve one system with scalar arithmetic or several systems with SIMD arithmetic.","title":"Tutorial 1 — Vectorizing a tridiagonal solve"},{"category":"section","location":"tutorials/thomas/#1.-Find-the-dependency","page":"1 · Tridiagonal recurrence","text":"For a tridiagonal system Ax=b, forward elimination computes each value from the one just before it. The backward pass has the same problem in reverse. Reordering either loop changes the algorithm.\n\n(Image: The main, upper, and lower diagonals of the system)\n\nusing Interleave\n\nfunction thomas!(x, d, u, l, b, scratch)\n    @inbounds begin\n        pivot = d[1]\n        invpivot = inv(pivot)\n        x[1] = b[1] * invpivot\n\n        for i in 2:length(x)\n            scratch[i] = u[i - 1] * invpivot\n            pivot = d[i] - l[i] * scratch[i]\n            x[i] = b[i] - l[i] * x[i - 1]\n            invpivot = inv(pivot)\n            x[i] *= invpivot\n        end\n\n        for i in length(x)-1:-1:1\n            x[i] -= scratch[i + 1] * x[i + 1]\n        end\n    end\n    x\nend\nthomas! (generic function with 1 method)\n\nThis is deliberately ordinary scalar-looking Julia. There are no intrinsics, lane loops, generated functions, or vector-specific branches. It is also non-divergent: every lane uses the same loop bounds and follows the same control flow. Values differ between systems, but no packed value decides whether only some lanes enter a branch.\n\n@inbounds is not needed to express the algorithm. It is a promise made by the kernel author after proving the indices valid; that promise is lexical, so apply! cannot safely add it around an arbitrary callback. On the measured Thomas workload, leaving the checks enabled still emitted SIMD but cost about 30%. A useful workflow is therefore to develop and test without @inbounds, then add it to the proven loop nest.","title":"1. Find the dependency"},{"category":"section","location":"tutorials/thomas/#2.-Build-a-scalar-oracle","page":"1 · Tridiagonal recurrence","text":"Put the batch in the first dimension. instance(A, b) is then the bth independent system.\n\nfunction tridiagonal_batch(Arr, nbatch, n)\n    x = Arr([0.0f0 for _ in 1:nbatch, _ in 1:n])\n    d = Arr([2.0f0 for _ in 1:nbatch, _ in 1:n])\n    u = Arr([-1.0f0 for _ in 1:nbatch, _ in 1:n])\n    l = Arr([-1.0f0 for _ in 1:nbatch, _ in 1:n])\n    b = Arr([sinpi(Float32(job) / 8) + Float32(i) / n\n             for job in 1:nbatch, i in 1:n])\n    x, d, u, l, b\nend\n\nxs, ds, us, ls, bs = tridiagonal_batch(Base.Array{Float32,2}, 61, 32)\napply!(thomas!, xs, ds, us, ls, bs; scratch=scratchlike(xs))\nxs[1, 1:4]\n4-element Vector{Float32}:\n 11.7895975\n 23.165262\n 34.09574\n 44.549786\n\napply! calls the kernel once per scalar instance. This path is valuable even if you never use SIMD: it is the simplest oracle for tests and debugging.\n\nThe comprehension form works with both aliases. For a packed array it first creates a scalar matrix and then copies it into DLI storage. That is ideal for examples and setup code. Use undef followed by broadcast or a loop when peak memory or initialization time matters.","title":"2. Build a scalar oracle"},{"category":"section","location":"tutorials/thomas/#3.-Switch-the-element-type","page":"1 · Tridiagonal recurrence","text":"Now choose packets of eight jobs. Sixty-one is intentionally not divisible by eight; the last packet therefore exercises padding.\n\nxv, dv, uv, lv, bv = tridiagonal_batch(Interleave.Array{Float32,2,8}, 61, 32)\napply!(thomas!, xv, dv, uv, lv, bv; scratch=scratchlike(xv))\n\n(xs == xv, packsize(xv), npacks(xv), npadding(xv))\n(true, 8, 8, 3)\n\nThe equality is exact, not approximate. Each lane performs the same floating-point operations, in the same order, as its scalar instance.\n\nwarning: Exactness has a contract\nDo not use @fastmath in a kernel that must match the scalar oracle. If fused multiply-add is part of the intended algorithm, write muladd explicitly in both paths.","title":"3. Switch the element type"},{"category":"section","location":"tutorials/thomas/#4.-See-why-it-vectorizes","page":"1 · Tridiagonal recurrence","text":"packet(xv, 1) is a dense vector whose element type is Vec{8,Float32}:\n\npacked = packet(xv, 1)\n(size(packed), strides(packed), eltype(packed))\n((32,), (1,), Vec{8, Float32})\n\nAt index i, x[i] therefore represents index i from eight systems. The recurrence continues along i, but each arithmetic operation advances eight unrelated systems.\n\n(Image: Data Layout Interleaving groups equal recurrence positions)\n\nThe final partial packet contains initialized padding lanes. They are allowed to compute, but the logical array hides them from indexing, comparison, and reduction.","title":"4. See why it vectorizes"},{"category":"section","location":"tutorials/thomas/#5.-Corroborate-performance-structurally","page":"1 · Tridiagonal recurrence","text":"A shorter runtime is not proof of SIMD: cache effects and instruction-level parallelism can also help. code_llvm asks Julia to show the typed LLVM intermediate representation produced for one concrete method signature. It does not run the solver and it is not code that belongs inside the application. Here is a compact Boolean check instead of a screenful of IR:\n\nusing InteractiveUtils\n\nfunction uses_vector_ir(f, argtypes, P)\n    io = IOBuffer()\n    code_llvm(io, f, argtypes; debuginfo=:none)\n    occursin(\"<$P x float>\", String(take!(io)))\nend\n\nuses_vector_ir(thomas!, NTuple{6,Vector{Vec{8,Float32}}}, 8) # true\n\nThe six argument types are x, d, u, l, b, and scratch. The marker <8 x float> means that the IR contains an eight-lane floating-point value. This is useful corroboration, not a permanent compiler guarantee: inspect native assembly as well when code generation is critical, and always pair structure with a runtime measurement.\n\nThen benchmark behind a function barrier. On the measured Apple M-series workload, packet sizes larger than the 128-bit hardware vector continued to help because several vectors in flight hide the latency of the dependency chain.\n\n(Image: Measured Thomas speedup by packet size)\n\nOn this run, P=32 reached 19.38× while P=16 reached 13.60×, even though one NEON register holds only four Float32 values. P=32 gives LLVM eight native vectors to schedule across the dependency latency. Going wider is not free: a separate P=64 inspection showed vector stack traffic, and the best threaded choice was P=16. The Thomas application study gives the complete sequential and threaded tables; Tutorial 4 develops a defensible tuning procedure.","title":"5. Corroborate performance structurally"},{"category":"section","location":"tutorials/thomas/#6.-Check-the-result-with-an-inverse-property","page":"1 · Tridiagonal recurrence","text":"Comparing with an oracle tests the implementation. Checking Ax-b also tests the problem being solved:\n\nfunction tridiagonal_mul!(r, d, u, l, x)\n    n = length(x)\n    @inbounds begin\n        r[1] = d[1] * x[1] + u[1] * x[2]\n        for i in 2:n-1\n            r[i] = l[i] * x[i - 1] + d[i] * x[i] + u[i] * x[i + 1]\n        end\n        r[n] = l[n] * x[n - 1] + d[n] * x[n]\n    end\n    r\nend\n\nr = Interleave.Array{Float32,2,8}(undef, 61, 32)\nfill!(r, 0)\napply!(tridiagonal_mul!, r, dv, uv, lv, xv)\nmaximum(abs(r[job, i] - bv[job, i]) for job in 1:61, i in 1:32) < 1f-4\ntrue\n\nYou now have the core Interleave workflow: validate on Base.Array, change only the batch type, demand exact lane-wise agreement, inspect LLVM, and measure several packet sizes.","title":"6. Check the result with an inverse property"},{"category":"section","location":"manual/kernels/#Writing-kernels","page":"Writing kernels","text":"A kernel operates on one scalar problem or one packet of problems. It never loops over the batch.","title":"Writing kernels"},{"category":"section","location":"manual/kernels/#The-basic-contract","page":"Writing kernels","text":"function kernel!(output, input, parameters...)\n    # iterate only over dimensions of one instance\n    output\nend\n\napply!(kernel!, output_batch, input_batch)\n\nAll batch arrays passed to one call must have the same logical batch size and packet size. Their instance shapes may differ when the algorithm intentionally uses different operands.","title":"The basic contract"},{"category":"section","location":"manual/kernels/#Let-values-carry-their-type","page":"Writing kernels","text":"Avoid hard-coded vector types in kernel code. Derive state from array elements:\n\nstate = zero(eltype(output))\ncoefficient = lanetype(eltype(output))(0.125)\n\nlanetype is useful for numeric literals because a scalar Float32 can multiply either a Float32 or a Vec{P,Float32}. It avoids accidentally introducing Float64 literals.","title":"Let values carry their type"},{"category":"section","location":"manual/kernels/#Preserve-operation-order","page":"Writing kernels","text":"Bit-exact scalar-to-packed comparison relies on executing the same operations in the same order. Therefore:\n\ndo not use @fastmath;\ndo not reorder sums for convenience;\ndo not replace division with a differently ordered reciprocal expression;\nuse muladd explicitly in both versions if fused multiply-add is intended.\n\nThis requirement is stronger than normal approximate numerical testing, but it catches lane mixups, layout errors, and padding leaks with a single oracle.","title":"Preserve operation order"},{"category":"section","location":"manual/kernels/#Keep-lane-control-flow-uniform","page":"Writing kernels","text":"Good kernels use lane-varying values with a common loop structure. Poor fits include:\n\nlane-dependent loop bounds;\nearly exit for only some jobs;\nfrequent branches selected by packed values;\ncross-lane reductions inside the recurrence.\n\nSome branches can be expressed with SIMD selection, but doing so is algorithm-specific and can waste most work when lanes diverge.\n\nA direct branch on packed data normally fails early rather than silently choosing one lane: x[i] > 0 produces Vec{P,Bool}, which is not the scalar Bool required by if. When both arms are safe to evaluate, SIMD.vifelse(mask, a, b) can select lane by lane, but both arms still contribute work. A branch controlled by a scalar parameter or an index is uniform and is fine.\n\nThe library cannot reliably “ban divergence” by inspecting Julia syntax. Divergence can be hidden behind a called function, and masked selection may be exactly the intended algorithm. The practical policy is: reject unsupported packed control flow through compilation tests, document intentional vifelse, and benchmark it. Lane-dependent loop counts or side effects remain outside the safe single-kernel model.","title":"Keep lane control flow uniform"},{"category":"section","location":"manual/kernels/#Allocate-scratch-once-per-driver-call","page":"Writing kernels","text":"Never allocate work arrays inside a kernel invoked once per packet. Pass a prototype through the driver:\n\nscratch = scratchlike(output)\napply!(kernel!, output, input; scratch)\n\nThe sequential driver creates one scratch array. The parallel driver creates one per chunk, preventing races without allocating once per packet. An explicit zero-argument callable is also accepted when construction needs more control.","title":"Allocate scratch once per driver call"},{"category":"section","location":"manual/kernels/#Demand-inference-and-zero-hot-path-allocations","page":"Writing kernels","text":"Useful regression checks are:\n\n@inferred packet(A, 1)\n@inferred apply!(kernel!, output, input)\n@allocated apply!(kernel!, output, input) == 0\n\nWarm the call before measuring allocations. Keep the measurement inside a function so global variables do not introduce boxing or dynamic dispatch.","title":"Demand inference and zero hot-path allocations"},{"category":"section","location":"manual/kernels/#Supported-element-operations-define-the-scope","page":"Writing kernels","text":"The packed specialization only works when every operation used by the kernel exists for SIMD.Vec. Basic arithmetic and many elementary patterns work naturally; arbitrary Julia objects, dynamically typed values, and automatic-differentiation dual numbers inside a Vec do not currently form a general solution.","title":"Supported element operations define the scope"},{"category":"section","location":"manual/gpu/#GPU-execution:-recurrence-per-work-item","page":"GPU execution","text":"The GPU translation keeps the mathematical idea and changes the machine mapping:\n\n CPU DLI GPU DLI\nsequential unit one Vec{P,T} recurrence one scalar recurrence per work item\nparallel population SIMD lanes a SIMD-group / wave of work items\nstorage Array{Vec{P,T}} scalar (nbatch, dims...) device array\ntuning knob packet size P workgroup size and resident batch size\n\nAt recurrence position i, GPU work items b, b+1, … read A[b, i], A[b+1, i], … . Julia stores the first dimension contiguously, so the population is coalesced without a packed element type. The recurrence is still sequential inside each work item.\n\nThis is the GPU analogue of the Interleave idea, but it is SIMT rather than explicit CPU SIMD. A device backend may still split or schedule a workgroup in hardware-specific ways; gpu_apply! does not promise a particular machine instruction width.","title":"GPU execution: recurrence per work item"},{"category":"section","location":"manual/gpu/#KernelAbstractions-tutorial","page":"GPU execution","text":"Install the vendor package in the application environment, then keep each batch as an ordinary scalar matrix before uploading it. The Julia kernel is unchanged across targets:\n\nusing Interleave, Metal                 # or CUDA / AMDGPU\n\nX, D, U, L, B = make_thomas_batch(Float32, nbatch, nx)\ndX, dD, dU, dL, dB = MtlArray.((X, D, U, L, B)) # CuArray / ROCArray on other targets\ndS = similar(dX)                    # one scratch row per independent problem\n\ngpu_apply!(thomas!, dX, dD, dU, dL, dB;\n           scratch=dS, workgroupsize=256)\n\n# Do more GPU work here; the launch above is asynchronous.\ngpu_synchronize(dX)\nresult = Array(dX)\n\nThe thomas! function is the same scalar Julia function used by apply!. The driver creates a small, allocation-free instance object inside every work item and passes those objects to the function. The same contract is exercised for Thomas, biquad, depthwise convolution, Sobel plus motion, Black–Scholes, a 3-D Laplacian, Thomas line sweeps, tridiagonal products, and reductions by the runnable gpu/ka/all.jl suite. Select a backend with INTERLEAVE_KA_BACKEND=metal, cuda, or amdgpu.\n\nThere is one important qualification to “the same kernel”: Metal's compiler accepts a restricted, statically dispatched subset of Julia. Device code cannot allocate, throw, perform I/O, create tasks, or call dynamically selected methods. Scalar CPU success is necessary, not sufficient, for GPU compilation.","title":"KernelAbstractions tutorial"},{"category":"section","location":"manual/gpu/#What-happened-to-P?","page":"GPU execution","text":"There is no P on the GPU path. Vec{P,T} is the CPU mechanism that forces several independent scalar values through one LLVM vector operation. On a GPU, separate work items already execute in lockstep. Nesting a Vec inside every work item would add a second level of vectorization, increase register pressure, and usually reduce occupancy.\n\nTune workgroupsize instead. Start at 128 or 256, benchmark neighbouring values, and include transfer costs only when the application really transfers for every call.","title":"What happened to P?"},{"category":"section","location":"manual/gpu/#Deferred-backends","page":"GPU execution","text":"Vulkan/SPIR-V is intentionally not part of the current API or continuous benchmark. The repository keeps the old gpu/vulkan/ directory as a historical ABI experiment, but no workflow compiles it and no result from it is presented as a KernelAbstractions execution. Reintroducing it would require a maintained Julia-to-SPIR-V compiler and a real host dispatcher, not merely another device-array alias.","title":"Deferred backends"},{"category":"section","location":"manual/gpu/#Continuous-benchmark-targets","page":"GPU execution","text":"The repository workflow mirrors the multi-target structure used by Legolas++:\n\nLinux x86-64 runs the complete BenchmarkTools CPU suite;\nmacOS 14 on Apple Silicon runs the same CPU suite and all validation kernels through the resident Metal KernelAbstractions driver;\nan optional NVIDIA CUDA job, dormant until a runner label is configured;\nan optional AMDGPU/ROCm job, dormant on the same terms.\n\nEach job uploads its raw output and appends it to the GitHub job summary.","title":"Continuous benchmark targets"},{"category":"section","location":"manual/gpu/#Obtaining-NVIDIA-numbers","page":"GPU execution","text":"warning: GitHub's managed GPU runners are not available here\nGitHub-hosted larger runners, GPU ones included, require an organization on a Team or Enterprise Cloud plan. LaurentPlagne/Interleave.jl is a personal account, so there is no runner to create and no label to copy. Earlier revisions of this page and of the workflow described a Tesla-T4 setup that cannot be performed on this repository.\n\nThree paths actually work, in increasing order of commitment:\n\nRun the suite by hand on any NVIDIA machine. This is what Legolas++ did: its published NVIDIA figures come from a workstation with a GeForce RTX 2060 SUPER, not from CI. gpu/run_remote.sh installs Julia if needed, records the device, resolves gpu/cuda, and runs the suite:\ngit clone https://github.com/LaurentPlagne/Interleave.jl && cd Interleave.jl\n./gpu/run_remote.sh cuda\nIt works unchanged on a rented box (RunPod, Vast.ai, Lambda), on a Colab runtime, and on an institutional workstation. Only the vendor driver is required: CUDA.jl ships its own toolkit as Julia artifacts.\nRegister a self-hosted runner under Settings → Actions → Runners, then set the repository variable INTERLEAVE_NVIDIA_RUNNER (or the nvidia_runner dispatch input) to its label. The ka-nvidia job then runs on releases and on a manual dispatch with run_gpu=true. A self-hosted runner on a public repository is only safe because this workflow has no pull_request trigger; do not add one.\nUse the JuliaGPU Buildkite infrastructure, which is how CUDA.jl and KernelAbstractions.jl test. It is free and permanent, but requires coordination on the JuliaLang Slack #gpu channel. See JuliaGPU/buildkite.\n\nThe same three options apply to AMDGPU through INTERLEAVE_AMD_RUNNER; GitHub publishes no AMD/ROCm hosted label at all.","title":"Obtaining NVIDIA numbers"},{"category":"section","location":"manual/gpu/#Correctness-contract","page":"GPU execution","text":"The GPU tests must preserve the properties that matter:\n\nno work item reads or writes another problem;\na non-multiple of the workgroup width is masked correctly;\nsequential and each configured KernelAbstractions backend are compared against the same scalar oracle;\nrepeated asynchronous launches are deterministic;\nno hidden host/device copy occurs in the timed region.\n\nCross-device bit identity should be tested but not advertised before it is observed on every supported backend. Different GPU division implementations and contraction rules may prevent the stronger CPU SIMD guarantee even when the source operation order is unchanged.","title":"Correctness contract"},{"category":"section","location":"manual/gpu/#When-a-GPU-is-the-wrong-target","page":"GPU execution","text":"GPU launch and transfer overhead can dominate short recurrences or small batches. The GPU path is attractive when the population is large, the data remains resident across several operations, and each problem exposes enough sequential work to amortize launch cost without exhausting registers. CPU DLI remains the low-latency path.","title":"When a GPU is the wrong target"},{"category":"section","location":"tutorials/choosing-p/#Tutorial-4-—-Decide-first,-then-tune-P","page":"4 · Decide and tune","text":"Interleave is a targeted optimization. This tutorial prevents the most common mistake: packing a kernel that LLVM already vectorizes well.","title":"Tutorial 4 — Decide first, then tune P"},{"category":"section","location":"tutorials/choosing-p/#1.-Measure-the-ordinary-kernel","page":"4 · Decide and tune","text":"Start from the Base.Array path and measure behind a function barrier. Record both runtime and a meaningful throughput. A low throughput is often evidence of a dependency chain; a high throughput suggests the compiler already found SIMD along a contiguous axis.\n\n(Image: Measured reference throughput predicts DLI benefit)\n\nThe separation in this dataset is striking, but it is not a universal numerical threshold. Use it as a question generator: inspect LLVM and identify the dependency that explains the measurement.","title":"1. Measure the ordinary kernel"},{"category":"section","location":"tutorials/choosing-p/#2.-Sweep-powers-of-two","page":"4 · Decide and tune","text":"Valid packet sizes are powers of two. Benchmark at least P ∈ (1, 2, 4, 8, 16, 32) when the element type and target allow it.\n\nfunction run_case(::Val{P}, setup, kernel!) where P\n    arrays = setup(Interleave.Array{Float32,2,P})\n    apply!(kernel!, arrays...)\n    arrays\nend\n\nKeep each timed case in its own function. Alternate variants rather than timing all runs of one variant and then all runs of another; this reduces bias from changing machine load.","title":"2. Sweep powers of two"},{"category":"section","location":"tutorials/choosing-p/#3.-Understand-why-P-can-exceed-hardware-width","page":"4 · Decide and tune","text":"On a 128-bit NEON unit, one native vector holds four Float32 values. Yet P=32 was best for the measured sequential Thomas solve:\n\n(Image: Thomas speedup for packet sizes one through thirty-two)\n\nA recurrence is latency-bound. A larger packet type can compile into several hardware vectors, giving the processor independent instructions to keep in flight while earlier operations wait. P is therefore an unrolling and latency-hiding parameter as well as a lane count.\n\nThe optimum can move when threads are enabled: the same Thomas problem preferred P=16 with ten Julia threads. At P=64, native-code inspection showed vector stack traffic and no further sequential gain. Sweep wide enough to observe both improvement and its plateau; do not assume the largest supported packet wins.","title":"3. Understand why P can exceed hardware width"},{"category":"section","location":"tutorials/choosing-p/#4.-Treat-P1-as-a-real-outcome","page":"4 · Decide and tune","text":"packtype(T, Val(1)) is exactly T, not Vec{1,T}. This matters: a one-lane vector can block LLVM from auto-vectorizing an otherwise ordinary contiguous loop.\n\nFor a stencil, convolution, or map-like kernel with no recurrence, the correct result of tuning may be P=1. That is not a failure. It means the standard compiler strategy already matches the problem.","title":"4. Treat P=1 as a real outcome"},{"category":"section","location":"tutorials/choosing-p/#5.-Add-threads-only-after-SIMD-tuning","page":"4 · Decide and tune","text":"Once the best sequential P is known, measure parallel_apply!. SIMD and threads do not necessarily multiply:\n\na compute-bound repeated solve may scale well across cores;\na streaming solve that reads several arrays may hit memory bandwidth early;\ntask-launch overhead dominates small batches.\n\nAlways compare these three configurations:\n\nscalar reference;\nbest sequential apply! configuration;\nthe same P with parallel_apply!.\n\nIf the third result is not optimal, repeat the P sweep with threading enabled. The sequential optimum is only a starting point because memory bandwidth and register pressure change when several cores execute packets concurrently.","title":"5. Add threads only after SIMD tuning"},{"category":"section","location":"tutorials/choosing-p/#6.-Automate-the-protocol,-not-the-decision-context","page":"4 · Decide and tune","text":"An opt-in tuner can construct all packet layouts, check each one against the scalar oracle, inspect LLVM, benchmark through BenchmarkTools, and return the fastest valid candidate. What it cannot infer is whether setup and packing belong in the timed operation, which problem sizes are representative, or whether exact equality is the appropriate numerical contract.\n\nCache a tuned choice only with its context: kernel type, scalar type, shape class, thread mode, Julia/LLVM version, and CPU. Because P determines the array's concrete storage type, selection must happen before production arrays are allocated; it should not be an invisible branch inside apply!.","title":"6. Automate the protocol, not the decision context"},{"category":"section","location":"tutorials/choosing-p/#7.-Use-a-stop-rule","page":"4 · Decide and tune","text":"Keep DLI only when all of the following are true:\n\nthe end-to-end workload improves, including layout conversion;\nLLVM contains the expected vector types;\npacked results exactly match the scalar oracle where exactness is required;\nthe best configuration remains stable across representative sizes;\nthe added memory footprint and padding are acceptable.\n\nIf any condition fails, retain the same kernel and select P=1 or a normal array.","title":"7. Use a stop rule"},{"category":"section","location":"applications/#Benchmark-applications","page":"What the suite demonstrates","text":"The benchmark suite is not a gallery of victories. It is a controlled experiment: the recurrence kernels expose the dependency Interleave targets, while stencils and reductions show where ordinary compiler SIMD is already a strong baseline.\n\napplication dependency inside one instance ordinary reference best Interleave configuration conclusion\nThomas tridiagonal solve forward and backward recurrences 1.1 GFlop/s P=32, 19.38× strong fit\nIIR biquad bank two-sample feedback state 2.9 GFlop/s P=16, 15.19× strong fit\nBlack–Scholes Crank–Nicolson time loop containing a Thomas solve 1.8 GFlop/s P=32, 17.51× strong fit\nDepthwise 3×3 convolution none between output pixels 41.1 GFlop/s P=1, 1.03× keep ordinary SIMD\nSobel plus motion none between output pixels 43.1 GFlop/s P=1, 0.99× packing is not the gain\nPer-instance reductions reduction within each instance control case measured P compare against ordinary compiler SIMD\n\n(Image: Reference throughput versus measured DLI speedup)\n\nThe useful discriminator is not “finance versus audio versus images.” It is whether the ordinary inner loop has a loop-carried dependency. The three slow references cannot use their natural iteration axis for SIMD, so Interleave uses independent instances instead. The two fast references already give LLVM a regular spatial loop; changing the SIMD direction merely replaces working vectorization with a less favourable one.","title":"Benchmark applications"},{"category":"section","location":"applications/#How-these-numbers-were-produced","page":"What the suite demonstrates","text":"All tables in this section are one local measurement, recorded on 15 September 2026:\n\nitem value\nCPU Apple M1, 128-bit NEON\nJulia 1.13.0\narithmetic Float32\nJulia threads 10\npacket sizes P ∈ (1, 2, 4, 8, 16, 32)\ntiming engine BenchmarkTools.jl, one interpolated sample per variant and round\ntiming protocol warm every variant, then five interleaved rounds; report the minimum\n\nThe scalar reference, all sequential packet sizes, and all threaded packet sizes are run in every round. Throughput uses the operation counts documented in bench/; it is an estimate, not a hardware-counter measurement. Machine load is printed beside every result and should be retained whenever the measurements are published.\n\nTwo comparisons answer different questions:\n\ncompare P>1 sequentially with the scalar reference to isolate the DLI idea;\ncompare threaded P>1 with threaded P=1 to estimate what packing adds after task parallelism is already present.\n\nThe “threaded versus scalar reference” column deliberately combines both effects. It is an end-to-end result, not evidence that SIMD and thread gains multiply independently.\n\nRun the same suite from the package environment with:\n\njulia --project=bench -t auto bench/runall.jl\n\nRun it on an otherwise quiet machine and keep the printed load average with the results. The harness uses BenchmarkTools.jl, checks the generated LLVM for the sequential Thomas, biquad, and depthwise packet kernels, and keeps every case behind a function barrier. Correctness and allocation checks remain in the test suite rather than being inferred from timings.","title":"How these numbers were produced"},{"category":"section","location":"applications/#Read-the-suite-as-a-decision-tool","page":"What the suite demonstrates","text":"Start with the ordinary Base.Array path and measure it.\nLocate the dependency, if there is one.\nSweep P; do not equate it with the hardware vector width.\nAdd task parallelism only after understanding the sequential result.\nInclude layout conversion in an application-level measurement.\n\nThe case studies show each step with the actual benchmark kernel. For a cross-cutting comparison with compiler vectorization, explicit SIMD, loop transformers, domain libraries, and GPUs, see Positioning and alternatives.","title":"Read the suite as a decision tool"},{"category":"section","location":"manual/tuning/#Performance-guide","page":"Performance","text":"","title":"Performance guide"},{"category":"section","location":"manual/tuning/#Begin-with-a-hypothesis","page":"Performance","text":"Interleave should solve a specific performance problem: an important loop is sequential because of a dependency, and many independent copies of that loop are available. Without both conditions, DLI is unlikely to help.\n\n(Image: Measured kernel fit map)\n\nThe measured examples divide into two regimes:\n\nkernel scalar reference best sequential DLI interpretation\nThomas 1.1 GFlop/s 19.38× at P=32 recurrence-limited\nIIR biquad 2.9 GFlop/s 15.19× at P=16 recurrence-limited\nBlack–Scholes CN 1.8 GFlop/s 17.51× at P=32 nested recurrence\ndepthwise 3×3 41.1 GFlop/s 1.03× at P=1 already vectorized\nSobel + motion 43.1 GFlop/s 0.99× at P=1 already vectorized\n\nThese are measurements from one Apple M1 system using Julia 1.13.0 and Float32, recorded on 15 September 2026. They explain a mechanism; they are not portable performance guarantees. The benchmark applications give the complete packet-size and threaded tables.","title":"Begin with a hypothesis"},{"category":"section","location":"manual/tuning/#Tune-P-empirically","page":"Performance","text":"Packet size must be a power of two. Test several values rather than equating P with the hardware SIMD width.\n\n(Image: Thomas speedup by packet size)\n\nFor latency-bound recurrences, a wide packet can expose several native vectors and hide instruction latency. For bandwidth-bound kernels, the same choice can increase pressure without adding useful work.\n\nThe Thomas sweep is a useful warning against stopping at P=16: sequential P=32 reached 19.38×, but threaded P=16 was faster than threaded P=32. A P=64 assembly audit then showed vector stack traffic. Include at least (1, 2, 4, 8, 16, 32) initially, inspect the code around the optimum, and tune again when the scheduler, data size, CPU, or precision changes.","title":"Tune P empirically"},{"category":"section","location":"manual/tuning/#Benchmark-defensibly","page":"Performance","text":"Put every measured case behind a function barrier.\nUse BenchmarkTools and interpolate the callable or measured arguments.\nWarm all variants before collecting results.\nAlternate variants and retain the minimum or a robust distribution.\nRecord machine load before timing.\nReport problem size, precision, P, thread count, and reference layout.\nMeasure allocations separately; a hot kernel should allocate zero bytes.\nInspect LLVM for <P x float> or the corresponding vector type.\n\nThe benchmark environment keeps BenchmarkTools out of the package's runtime dependencies:\n\njulia --project=bench -t auto bench/runall.jl\n\nbench/harness.jl builds interpolated @benchmarkable objects, takes one sample from every variant in turn, and repeats the round. That preserves BenchmarkTools' measurement machinery without assigning slow machine drift systematically to the variant measured last.\n\nBoth runtime and structure matter. A vector type in LLVM does not prove a speedup; a speedup alone does not prove vectorization.","title":"Benchmark defensibly"},{"category":"section","location":"manual/tuning/#What-can-be-automated","page":"Performance","text":"Correctness, code-generation inspection, and selection can all be automated, but only after the caller supplies the missing semantics:\n\ncheck what the tool can do what the caller must provide\nnumerical validity compare every logical value and exclude padding scalar oracle and == or another comparator\nvectorization search typed LLVM for vector values and optionally inspect native code concrete kernel signature and target CPU\nbest P benchmark candidates and return the fastest valid one representative setup, sizes, scheduler, and conversion costs\nregression compare saved benchmark results across revisions stable hardware and an acceptable threshold\n\nA future opt-in autotune(setup, kernel!; packs=(1,2,4,8,16,32)) layer would therefore be reasonable. It should allocate and validate every candidate, measure through BenchmarkTools, and cache by kernel type, scalar type, problem-size class, thread mode, Julia/LLVM version, and CPU. It should not make P=:auto a hidden constructor behaviour: P determines the storage type before allocation, tuning has a visible cost, and the best sequential and threaded layouts can differ.\n\nThe LLVM string check used by the current harness is a regression diagnostic, not a public compiler contract. Numerical exactness remains the hard invariant.","title":"What can be automated"},{"category":"section","location":"manual/tuning/#Compare-with-Legolas","page":"Performance","text":"For the same 4,096-by-64 Thomas workload and with FMA contraction disabled on the C++ side, Julia was within 3% of Legolas++ at P=8, 1% at P=16, and 0.7% at P=32. Both generated one, two, four, and eight NEON instruction groups for P=4 through P=32, with no vector spills at those sizes. See the Thomas parity audit and its reproducible C++ driver rather than treating either runtime alone as proof.","title":"Compare with Legolas++"},{"category":"section","location":"manual/tuning/#Continuous-integration-versus-performance-tracking","page":"Performance","text":"GitHub-hosted public runners currently cover Linux x86-64 and Arm64, Windows x86-64 and Arm64, and macOS Intel and Apple Silicon. Use that matrix for correctness, inference, allocation, and target-specific code-generation smoke tests. It is also useful for catching an unsupported vector type or ABI assumption. Runner availability is not the same as a mature Julia toolchain: setup-julia still describes its aarch64 path as untested, so bring up Arm jobs as experimental entries and pin a configuration that has actually installed Julia successfully on each image.\n\nDo not turn noisy shared-runner timings into a hard pass/fail performance guarantee. Store their results as observational artifacts or compare only generous relative bounds. For actionable regression thresholds, use a quiet, pinned self-hosted machine; PkgBenchmark and BenchmarkCI can compare revisions while the existing bench/ environment remains the suite of record.","title":"Continuous integration versus performance tracking"},{"category":"section","location":"manual/tuning/#Include-layout-cost","page":"Performance","text":"If upstream data arrives in another arrangement, measure:\n\npack or transpose + all packed kernels + unpack or transpose back\n\nDLI is easiest to justify when the packed representation is native to the application or is reused over many recurrence steps.","title":"Include layout cost"},{"category":"section","location":"manual/tuning/#Diagnose-scaling-limits","page":"Performance","text":"Near-linear improvement with P suggests latency or instruction throughput was limiting.\nA plateau across larger P can indicate memory bandwidth, register pressure, or too little work per packet.\nPoor P=1 performance from Vec{1,T} is avoided by design: Interleave uses scalar T.\nWeak thread scaling after strong SIMD scaling often indicates shared memory bandwidth.\n\nSee Tutorial 4 for the full decision procedure.","title":"Diagnose scaling limits"},{"category":"section","location":"manual/review/#Design-review-and-roadmap","page":"Design review and roadmap","text":"This note records the design review of Interleave.jl and the corrective work applied after it. It is deliberately separate from the user tutorial: it describes the current contracts, their limits, and the evidence still required before calling the API stable.","title":"Design review and roadmap"},{"category":"section","location":"manual/review/#Assessment","page":"Design review and roadmap","text":"The central design is sound. A scalar kernel is applied to independent instances; on the CPU, the machine element changes from T to Vec{P,T}, while on a GPU one work item owns one complete scalar instance. apply!, parallel_apply!, and gpu_apply! make the three execution choices explicit. This imports the idea of Legolas++ without importing its C++ expression-template machinery.\n\nThe promise should nevertheless be stated precisely:\n\nWrite the calculation of one independent instance once; adapt storage and execution to the target.\n\nIt should not promise that every kernel is automatically optimal on every backend. A recurrence such as Thomas or a biquad maps naturally to one GPU work item. A 2-D stencil needs an additional decomposition over pixels or tiles; the current one-instance-per-work- item driver proves source reuse, but is not yet a performance-optimal stencil schedule.","title":"Assessment"},{"category":"section","location":"manual/review/#Findings-fixed-in-this-revision","page":"Design review and roadmap","text":"Interleave.Array stores a packed data array and an aliased scalar flat view. The generic deepcopy implementation copied those fields independently. Scalar indexing could therefore return 7 while the packed kernel still read 1. A custom Base.deepcopy_internal now rebuilds flat from the copied packed storage, and the test suite checks the invariant.\nSerialization had the same defect for the same reason, and it had been missed: a serialize/deserialize round-trip returned an array whose scalar writes were invisible to every kernel. InterleaveSerializationExt now writes only the packed storage and the batch size, leaving the constructor as the single place that establishes the alias. The hazard is structural rather than incidental: any field-wise reconstruction reproduces it, so JLD2, BSON, and Arrow remain unsupported until given the same treatment.\nCPU scratch is an instance-sized prototype; GPU scratch is a batch-major device array. The Metal host oracle now constructs an instance-sized CPU scratch view, so it no longer accidentally validates a linear slice of the GPU buffer.\nAll in-place CPU benchmark variants can now receive reset callbacks. Each one-evaluation sample starts from the same state, while reset work remains outside the timed region. This matters particularly for Black–Scholes, where V and RHS are overwritten.\nThe shared KernelAbstractions suite resets resident device buffers before every timed sample. Its timings remain launch-plus-device timings, but no longer measure a different numerical problem on each repetition.\nThe benchmark workflow explicitly selects bash. This enables pipefail, so a Julia failure cannot be hidden by a successful tee command.\nThe C++ audit driver now points at the sibling Legolas++ checkout and compiles against its actual Legolas::Array/Legolas::map API. A small local run was used as a smoke test; it is still not a cross-language performance claim.","title":"Findings fixed in this revision"},{"category":"section","location":"manual/review/#API-points-to-stabilize","page":"Design review and roadmap","text":"scratch should eventually have an explicit preparation object or a documented distinction between a CPU prototype and a GPU allocation. A reusable execution plan could own the workspace, backend, workgroup size, and (for CPU) packet size, avoiding repeated setup while keeping apply! allocation-free in its hot loop.\n\nResolved. instance(A, k) used to mean a packet, so k ranged over 1:npacks on an interleaved array but over 1:nbatch on a Base.Array — the same call denoting P problems or one, with no visible difference in the returned shape. The hot path is now packet, and instance(A, b) always denotes exactly problem b, reading one lane out of each packet. It is a debugging, comparison and I/O tool, not a hot path.\n\nFinally, parameters such as filter coefficients should be ordinary concrete arguments or a small callable object. Backend-specific gpu_* wrappers are useful as smoke-test fixtures, but should not become a second user kernel language.","title":"API points to stabilize"},{"category":"section","location":"manual/review/#KernelAbstractions-decision","page":"Design review and roadmap","text":"KernelAbstractions is the right portability layer for the GPU path. Its documented backends include CUDA, ROCm, oneAPI, and Metal, while backend-specific packages still provide the actual compiler and runtime. The optional extension design keeps Interleave usable without a GPU dependency.\n\nThe CPU SIMD path should remain separate: KA provides a common GPU-like execution model, not the best implementation of the native Vec{P,T} storage path. Likewise, GPU compilation is only guaranteed for the backend-supported Julia subset (no allocation, exceptions, I/O, tasks, or unconstrained runtime dispatch).","title":"KernelAbstractions decision"},{"category":"section","location":"manual/review/#Recommended-next-milestones","page":"Design review and roadmap","text":"Finish the container audit. copy, deepcopy, and Serialization are now covered by tests; views, the garbage-collection lifetime of the unsafe_wrap alias, and third-party serializers (JLD2, BSON, Arrow) are not.\nStrengthen cross-backend tests with nonuniform data, non-multiple batch sizes, boundary cases, and independent scratch contents. Done for the shared KernelAbstractions suite and the CPU-backend GPU testsets: constant inputs made several comparisons vacuous (a Sobel filter of a constant image is zero everywhere, and a constant image is invariant under an i/j swap), and every batch size was a multiple of the work-group width, so the masking guard was never exercised. Independent scratch contents remain to be covered.\nExtend the repaired C++ comparison harness with identical problem sizes, operation counts, compiler flags, device information, transfers, and launch synchronization in every report.\nObtain CUDA (and ideally AMDGPU) results and compare them with Metal. This is the meaningful test of “write once, execute on several targets”. Note that GitHub's managed GPU runners are unavailable to this repository, so this goes through gpu/run_remote.sh on a borrowed or rented machine, a self-hosted runner, or JuliaGPU Buildkite.\nAdd explicit execution plans and measured tuning for P, CPU chunking, and GPU workgroup size. For stencils, introduce a separate pixel/tile execution domain rather than duplicating the numerical formula.\n\nThe project is therefore ready for continued development, but not yet for a broad claim of performance portability. Its strongest current result is a credible single-source model; the next work should make the measurements and contracts as reliable as that model.","title":"Recommended next milestones"},{"category":"section","location":"manual/dimensions/#Dimensions-and-the-packed-axis","page":"Dimensions and packed axis","text":"The most important layout decision is not packet size. It is choosing what one lane means.","title":"Dimensions and the packed axis"},{"category":"section","location":"manual/dimensions/#The-first-dimension-is-the-population","page":"Dimensions and packed axis","text":"For every Interleave batch:\n\nsize(A) == (number of jobs, dimensions of one job...)\n\nExamples:\n\napplication logical shape one lane represents\nfilter bank (channels, samples) one channel\ntridiagonal ensemble (systems, unknowns) one linear system\nimage-channel operation (channels, height, width) one channel\nparameter study (parameters, state_variables) one parameter set\nx-sweep in ADI (y_lines, x, z) one y-position\n\nInterleave packs consecutive values of the first logical dimension. The remaining dimensions become the dense instance presented to the kernel.","title":"The first dimension is the population"},{"category":"section","location":"manual/dimensions/#Choose-independence-before-locality","page":"Dimensions and packed axis","text":"The packed axis must identify independent jobs. If lane 3 reads or writes lane 4, the model is wrong. Once independence is established, prefer an axis that:\n\nsupplies enough jobs to fill packets;\ngives jobs similar control flow and runtime;\nlets the packed layout live across several kernel calls;\nminimizes transposition or packing cost.\n\nThe largest axis is not automatically best. A smaller but more uniform population may waste less masked or divergent work.","title":"Choose independence before locality"},{"category":"section","location":"manual/dimensions/#Column-major-Julia-and-the-scalar-oracle","page":"Dimensions and packed axis","text":"The batch-first logical convention makes a standard Julia instance such as view(A, job, :) strided. This is intentional: the public shape remains identical when the type changes, while Interleave.Array rearranges physical storage so a packed instance is contiguous.\n\nThis distinction matters when benchmarking. Compare full application paths and state the layout of the scalar reference. A batch-last scalar array may be a faster hand-optimized reference, but it no longer has the same constructor and indexing contract.","title":"Column-major Julia and the scalar oracle"},{"category":"section","location":"manual/dimensions/#Multidimensional-instances","page":"Dimensions and packed axis","text":"An instance can be a matrix or a volume. packet(A, k) removes only the packet dimension:\n\nusing Interleave\n\nA = Interleave.Array{Float32,4,8}(undef, 33, 10, 12, 14)\n(size(A), instance_size(A), size(instance(A, 1)), size(parent(A)))\n((33, 10, 12, 14), (10, 12, 14), (10, 12, 14), (10, 12, 14, 5))\n\nThe kernel should traverse those dimensions in Julia's natural column-major order whenever the recurrence permits it.","title":"Multidimensional instances"},{"category":"section","location":"manual/dimensions/#Partial-packets","page":"Dimensions and packed axis","text":"For nbatch=33 and P=8, Interleave allocates five packets and seven padding lanes. The logical size remains 33. Padding affects memory and executed instructions but never scalar indexing or reductions.\n\nIf partial packets dominate—say, three jobs with P=16—choose a smaller P or use the standard array path.","title":"Partial packets"},{"category":"section","location":"applications/black-scholes/#Black–Scholes-with-Crank–Nicolson","page":"Black–Scholes Crank–Nicolson","text":"This benchmark prices a population of options with a one-dimensional finite-difference scheme. Each time step has two parts: build the explicit tridiagonal right-hand side, then solve the implicit system with Thomas. The time steps are sequential, and the Thomas sweep inside each step is sequential too.\n\n(Image: Explicit stencil and implicit Thomas sweep for several options)\n\nInterleave assigns one option grid to each lane. It does not parallelise time or alter the finite-difference scheme. All lanes execute the same time step and grid point while keeping their values independent.","title":"Black–Scholes with Crank–Nicolson"},{"category":"section","location":"applications/black-scholes/#The-benchmark","page":"Black–Scholes Crank–Nicolson","text":"bench/optionpricing.jl advances 16,384 option grids, each with 64 points, through 32 time steps. The coefficient arrays are deliberately per-option, so the example also exercises a real batched data layout. The estimate is 15 operations per grid point and time step.\n\nconfiguration time estimated GFlop/s speedup vs scalar\nscalar reference 273.37 ms 1.8 1.00×\nP=1 273.39 ms 1.8 1.00×\nP=2 145.63 ms 3.5 1.88×\nP=4 73.34 ms 6.9 3.73×\nP=8 40.11 ms 12.5 6.82×\nP=16 23.16 ms 21.7 11.80×\nP=32 15.61 ms 32.2 17.51×\n\nThe improvement is slightly less than proportional to P, but the repeated use of each option grid gives DLI enough arithmetic to amortise the packed representation.","title":"The benchmark"},{"category":"section","location":"applications/black-scholes/#With-explicit-task-parallelism","page":"Black–Scholes Crank–Nicolson","text":"configuration time estimated GFlop/s speedup vs scalar speedup vs threaded P=1\nP=1, 10 threads 36.06 ms 14.0 7.58× 1.00×\nP=2, 10 threads 19.70 ms 25.6 13.88× 1.83×\nP=4, 10 threads 10.67 ms 47.2 25.62× 3.38×\nP=8, 10 threads 6.41 ms 78.6 42.66× 5.63×\nP=16, 10 threads 4.07 ms 123.8 67.24× 8.87×\nP=32, 10 threads 2.85 ms 176.7 95.95× 12.66×\n\nThis is the suite's strongest threaded case. The 32 time steps repeatedly reuse a small grid, so the computation is more cache-friendly and compute-bound than the one-pass Thomas benchmark. The 95.95× figure is still one local end-to-end measurement, not a multiplication of independent 10-thread and 32-lane factors.","title":"With explicit task parallelism"},{"category":"section","location":"applications/black-scholes/#Competing-approaches","page":"Black–Scholes Crank–Nicolson","text":"For a vanilla European option under the exact Black–Scholes assumptions, the closed-form formula removes the PDE entirely and should win. A finite-difference solver is justified by contracts, boundaries, coefficients, or exercise features that require it.\nMethodOfLines.jl automates finite-difference discretisation of symbolic PDEs and connects to the SciML solver ecosystem. Prefer that route for modelling flexibility and solver composition; prefer Interleave when a compact, fixed CPU kernel and its exact execution order are central.\nA GPU can expose both option-level and grid-level parallelism. CUDA.jl supports high-level array operations and custom Julia kernels; cuSPARSE supplies batched tridiagonal solves. This is compelling for device-resident portfolios large enough to amortise transfers and launches.\nReplacing Thomas with cyclic reduction or PCR exposes parallelism within each grid, but it changes work, storage, and floating-point order. Interleave instead keeps the numerical kernel unchanged and exploits independence between options.\n\nThe application is therefore a good Interleave target only after the modelling choice has been made: DLI optimises a chosen recurrence; it does not decide whether the recurrence is the right pricing method.","title":"Competing approaches"},{"category":"section","location":"reference/#API-reference","page":"API reference","text":"","title":"API reference"},{"category":"section","location":"reference/#Batch-representation","page":"API reference","text":"","title":"Batch representation"},{"category":"section","location":"reference/#Traversal","page":"API reference","text":"packet and instance are deliberately distinct. A kernel is called on packets; instance names one logical problem and is the debugging, comparison and I/O path.","title":"Traversal"},{"category":"section","location":"reference/#Tuning","page":"API reference","text":"","title":"Tuning"},{"category":"section","location":"reference/#GPU-execution","page":"API reference","text":"","title":"GPU execution"},{"category":"section","location":"reference/#Schedulers","page":"API reference","text":"Interleave re-exports these scheduler types from OhMyThreads for use with parallel_apply!:\n\nSerialScheduler\nStaticScheduler\nDynamicScheduler\nGreedyScheduler\n\nSee the OhMyThreads scheduler documentation for their complete option reference.","title":"Schedulers"},{"category":"module","location":"reference/#Interleave","page":"API reference","text":"Interleave\n\nApply an intrinsically sequential algorithm—a recurrence—to a batch of independent problems. Data Layout Interleaving exposes SIMD parallelism across that batch, with optional and explicit task parallelism as a second level.\n\nDevelop the algorithm on ordinary multidimensional Base.Array objects. Write the kernel once in scalar notation; changing the element type through the P parameter of Interleave.Array selects the packed specialization.\n\nThe packed type expresses explicit SIMD lanes for supported operations. It does not guarantee optimal performance: the best P depends on the kernel and target machine and must be measured.\n\nDo not use @fastmath in a kernel that must match its scalar reference exactly. FMA contraction can break bit-exact agreement.\n\n\n\n\n\n","title":"Interleave"},{"category":"type","location":"reference/#Interleave.Array","page":"API reference","text":"Interleave.Array{T,N,P}\n\nAn N-dimensional scalar array whose first dimension is stored in packets of P using Data Layout Interleaving.\n\nThe first two parameters have the same meaning as for Base.Array: scalar element type T and number of dimensions N. P is the only addition, allowing the storage type to be changed in one line:\n\nconst Arr = Base.Array{Float32,2}        # development\nconst Arr = Interleave.Array{Float32,2,8}   # packed production layout\nX = Arr(undef, nsys, nx)                 # identical construction\n\nThe first dimension is the batch: A contains size(A, 1) independent problems, each with shape size(A)[2:end]. apply! invokes a kernel on those problems.\n\nA Interleave.Array is logically an array of scalars: size(A) == (nbatch, instance_dims...), eltype(A) === T, and A[b, i, j] returns a scalar. Fill, read, reduce, and compare it like any other AbstractArray.\n\nThe machine representation is available through parent(A): a dense array of packed elements with shape (instance_dims..., npacks). The driver slices this storage for the kernel. Normal user code does not need it.\n\nP fixes the machine element type (see packtype) and can change performance, but never changes the logical semantics.\n\nExample\n\nD = Array{Float32}(undef, 65_536, 64; pack = Val(8))\nD .= 2.0f0                 # ordinary broadcast with scalar indices\nD[7, 3] = 1.5f0            # scalar indexing\napply!(kernel!, D)         # the kernel receives packet views\n\n\n\n\n\n","title":"Interleave.Array"},{"category":"function","location":"reference/#Interleave.packtype","page":"API reference","text":"packtype(T, Val(P)) -> Type\n\nMachine element type for a batch interleaved in packets of P: T itself when P == 1, and Vec{P,T} otherwise.\n\nThe scalar special case matters. Vec{1,T} can prevent LLVM from vectorizing an ordinary contiguous loop. Returning T makes P=1 the normal dense-array regime, so one kernel can cover both recurrence-heavy and already-vectorizable workloads.\n\n\n\n\n\npacktype(A) -> Type\n\nMachine element type seen by a kernel. It is T when P == 1 and Vec{P,T} otherwise. This differs from the logical eltype(A), which is always scalar.\n\nUseful when constructing scratch storage directly. In most cases, similar(instance(A, 1)) is sufficient.\n\n\n\n\n\n","title":"Interleave.packtype"},{"category":"function","location":"reference/#Interleave.packsize","page":"API reference","text":"Number of lanes in one packet (the layout parameter P).\n\n\n\n\n\n","title":"Interleave.packsize"},{"category":"function","location":"reference/#Interleave.npacks","page":"API reference","text":"Number of packets, equal to cld(A.nbatch, packsize(A)).\n\n\n\n\n\n","title":"Interleave.npacks"},{"category":"function","location":"reference/#Interleave.npadding","page":"API reference","text":"Number of non-significant padding lanes (npacks*P - nbatch).\n\n\n\n\n\n","title":"Interleave.npadding"},{"category":"function","location":"reference/#Interleave.instance_size","page":"API reference","text":"Shape of one problem instance, without the packet dimension.\n\n\n\n\n\n","title":"Interleave.instance_size"},{"category":"function","location":"reference/#Interleave.lanetype","page":"API reference","text":"lanetype(x) -> Type\n\nUnderlying scalar type of a batch element: Float32 for both Float32 and Vec{P,Float32}.\n\nUse it to construct a correctly typed scalar literal in a generic kernel. A scalar of the right type can multiply either a scalar or a SIMD packet without relying on conversion from a default Float64 literal.\n\nS = lanetype(eltype(x))\ny = v * S(0.125)\n\n\n\n\n\n","title":"Interleave.lanetype"},{"category":"method","location":"reference/#Base.parent-Tuple{Interleave.Array}","page":"API reference","text":"parent(A) -> Array\n\nThe batch's machine representation: a dense array of packed elements with shape (instance_dims..., npacks). apply! and parallel_apply! slice this storage; A itself remains scalar-indexed.\n\n\n\n\n\n","title":"Base.parent"},{"category":"function","location":"reference/#Interleave.packet","page":"API reference","text":"packet(A, k)\n\nView of the kth packet of problems: a dense SubArray with the shape of one instance and machine element type Vec{P,T} (or T for a standard array). This is what a kernel receives; the driver owns batch traversal.\n\nk runs from 1 to npacks, not to size(A, 1). One packet holds packsize problems. For a single logical problem, use instance.\n\n\n\n\n\n","title":"Interleave.packet"},{"category":"function","location":"reference/#Interleave.instance","page":"API reference","text":"instance(A, b)\n\nView of the bth individual problem, with scalar element type T and the shape of one instance. b runs from 1 to size(A, 1).\n\nThis is the honest counterpart of packet: it always denotes exactly one problem, whatever the storage. On an interleaved array it reads one lane out of each packet, so it is a debugging, comparison, and I/O tool rather than a hot path. Kernels receive packets.\n\nA = Interleave.Array{Float32,2,8}(undef, 100, 16)\nsize(packet(A, 1))     # (16,) — holds 8 problems, k runs 1:13\nsize(instance(A, 1))   # (16,) — holds 1 problem,  b runs 1:100\n\n\n\n\n\n","title":"Interleave.instance"},{"category":"function","location":"reference/#Interleave.packs","page":"API reference","text":"packs(A) -> AbstractVector\n\nA lazy vector whose elements are the packet views of A—the values received by a kernel. It supports sequential traversal with Base.foreach:\n\nforeach(kernel!, packs(X), packs(D), packs(U))\n\nforeach is sequential, like apply!. It has no per-task scratch storage or scheduler; use parallel_apply! for those features.\n\n\n\n\n\n","title":"Interleave.packs"},{"category":"function","location":"reference/#Interleave.scratchlike","page":"API reference","text":"scratchlike(A) -> Array\n\nWorkspace prototype for one instance of batch A, ready to pass as the scratch keyword of apply! and parallel_apply!.\n\nIt replaces the cryptic similar(packet(A, 1)) idiom and is correct for an empty batch, where no packet exists to copy.\n\nwarning: CPU and GPU scratch are different objects\napply! wants one instance of workspace, which this returns. gpu_apply! wants a batch-major device array, one private row per work item — use gpu_scratchlike(A) from the KernelAbstractions extension. The two are not interchangeable, and the drivers reject the wrong one with an explicit message.\n\n\n\n\n\n","title":"Interleave.scratchlike"},{"category":"function","location":"reference/#Interleave.apply!","page":"API reference","text":"apply!(f, arrays...; scratch = nothing)\n\nApply kernel f to every problem in the batch, sequentially. SIMD comes from the machine element type rather than task parallelism.\n\nBoth Interleave.Array and ordinary Base.Array batches are accepted. For a standard array, the first dimension is the batch and each packet contains one lane, which makes it a convenient development and reference path.\n\nf receives one packet view per array (see packet). Packed views are dense; the reference Base.Array view may be strided because the batch is its first dimension. If scratch is supplied, it is appended as the last argument.\n\nwarning: Never launches tasks\napply! cannot parallelize: it has no scheduler or chunk-count keyword. Use parallel_apply! when task creation should be explicit at the call site.\n\nscratch: one instance of workspace, obtained with scratchlike. It is copied with similar, so a zero-argument callable is also accepted for custom construction. Scratch storage is never allocated inside the kernel. This is not the batch-major buffer that gpu_apply! expects; passing one is rejected with an explicit message.\n\nReturn the first array.\n\nExample\n\napply!(thomas!, X, D, U, L, B; scratch = scratchlike(X))\n\n\n\n\n\n","title":"Interleave.apply!"},{"category":"function","location":"reference/#Interleave.parallel_apply!","page":"API reference","text":"parallel_apply!(f, arrays...; scratch = nothing, scheduler = StaticScheduler(), nchunks = ...)\n\nLike apply!, but distribute packet chunks across tasks. Call it only when the surrounding context does not already own parallelism; the separate name keeps that decision visible.\n\nscheduler: an OhMyThreads scheduler (StaticScheduler, DynamicScheduler, GreedyScheduler, or SerialScheduler). Use chunking=false when exactly one task per chunk is required.\nscratch: an instance-sized prototype from scratchlike, copied with similar once per chunk, so every task owns its mutable workspace. Sharing one scratch object between tasks creates a race.\nnchunks: packet-partition granularity.\n\nReturn the first array.\n\n\n\n\n\n","title":"Interleave.parallel_apply!"},{"category":"type","location":"reference/#Interleave.PacketBoundsError","page":"API reference","text":"Interleave.PacketBoundsError\n\nRaised when a packet index falls outside 1:npacks(A). The message states where the bound comes from, because npacks is derived from the batch size and P rather than given.\n\n\n\n\n\n","title":"Interleave.PacketBoundsError"},{"category":"function","location":"reference/#Interleave.tune","page":"API reference","text":"tune(f, make; packs = (1, 2, 4, 8, 16, 32), rounds = 5, scratch = true) -> TuningResult\n\nMeasure kernel f at several packet sizes and report which one wins on this machine.\n\nmake(P) must build and return the tuple of arrays for packet size P, in the order f expects them. It is called again before every timed round, so in-place recurrences start each measurement from the same state and the rebuild stays outside the timed region.\n\nP is a layout parameter, not the hardware SIMD width: on a 128-bit NEON machine P = 16 routinely beats P = 4, because a recurrence is latency-bound and several vectors in flight hide the dependency chain. It has to be measured, which is what this function is for.\n\npacks: packet sizes to try. Each must be a power of two.\nrounds: timed repetitions per size; the minimum is kept.\nscratch: true allocates one instance of workspace with scratchlike, false calls the kernel without one, and anything else is passed through as the prototype.\n\nnote: A decision tool, not a publication measurement\ntune uses a plain minimum over a few rounds. It reliably separates variants that differ by tens of percent or more, which is what choosing P requires. For a number you intend to publish, use BenchmarkTools and corroborate the vectorization structurally, as bench/ does.\n\nExample\n\nresult = tune(thomas!, P -> setup_arrays(65_536, 64, P))\nresult.best        # 16\nresult[8]          # wall time at P = 8, in seconds\n\n\n\n\n\n","title":"Interleave.tune"},{"category":"type","location":"reference/#Interleave.TuningResult","page":"API reference","text":"TuningResult\n\nResult of tune: the measured packet sizes, the best wall time of each, and the one that won. show renders it as a table with speedups relative to P = 1 when that size was measured.\n\n\n\n\n\n","title":"Interleave.TuningResult"},{"category":"function","location":"reference/#Interleave.gpu_apply!","page":"API reference","text":"gpu_apply!(f, arrays...; scratch=nothing, workgroupsize=256, wait=false)\n\nSubmit one GPU work item per independent problem. Every array uses the ordinary logical shape (nbatch, dims...); because Julia is column-major, the batch axis is contiguous and accesses made by neighbouring GPU work items are coalesced.\n\nThe work item receives allocation-free AbstractArray views of one problem and calls f with the same signature as apply!. The recurrence inside f remains sequential. GPU SIMT execution across problems replaces the CPU's Vec{P,T} element type, so there is no packet size P on this path.\n\nIf scratch is needed, pass a batched device array, obtained with gpu_scratchlike. Each work item sees only its own slice. Interleave allocates no device or scratch buffer; the backend may still allocate compiler and launch bookkeeping on the host.\n\nLaunches follow normal GPU conventions and are asynchronous. Set wait=true for a blocking call, or call gpu_synchronize later. workgroupsize must be positive and should be measured on the target device.\n\nOnly the GPU-compilable subset of Julia may appear in f: no allocation, exceptions, I/O, task creation, or runtime dispatch. A kernel that works with apply! is therefore not automatically guaranteed to compile for every GPU backend.\n\nThe implementation is an optional package extension. Loading Interleave alone does not load a GPU compiler. Load Metal.jl or another KernelAbstractions-compatible backend before calling this function.\n\nExample\n\nusing Metal\n\ndX, dD, dU, dL, dB = MtlArray.((X, D, U, L, B))\ndS = similar(dX)\ngpu_apply!(thomas!, dX, dD, dU, dL, dB; scratch=dS, wait=true)\nX .= Array(dX)\n\n\n\n\n\n","title":"Interleave.gpu_apply!"},{"category":"function","location":"reference/#Interleave.gpu_scratchlike","page":"API reference","text":"gpu_scratchlike(A) -> device array\n\nBatch-major workspace for gpu_apply!: one private row per work item, on the same backend as A. Available once a KernelAbstractions backend is loaded.\n\nThis is deliberately not the same function as scratchlike, which returns one instance of CPU workspace for apply!. The two shapes are different and are not interchangeable; separate names make a mix-up impossible to write by accident.\n\n\n\n\n\n","title":"Interleave.gpu_scratchlike"},{"category":"function","location":"reference/#Interleave.gpu_backend","page":"API reference","text":"gpu_backend(A)\n\nReturn the execution backend associated with device array A. This method becomes available when a KernelAbstractions-compatible backend such as Metal.jl is loaded.\n\n\n\n\n\n","title":"Interleave.gpu_backend"},{"category":"function","location":"reference/#Interleave.gpu_synchronize","page":"API reference","text":"gpu_synchronize(A)\n\nWait until all previously submitted work on the backend associated with A has completed. gpu_apply! is asynchronous unless called with wait=true.\n\n\n\n\n\n","title":"Interleave.gpu_synchronize"},{"category":"section","location":"applications/thomas/#Thomas-tridiagonal-systems","page":"Thomas tridiagonal systems","text":"The Thomas algorithm solves a tridiagonal system in linear time. It is compact, cache friendly, and intrinsically sequential inside one system: each row in the forward elimination depends on the preceding row, and back substitution has the reverse dependency.\n\n(Image: Scalar Thomas chains and SIMD across independent systems)\n\nInterleave leaves both sweeps intact. Lane 1 solves system 1, lane 2 solves system 2, and so on. At row i, one vector operation advances the same row of P unrelated systems.","title":"Thomas tridiagonal systems"},{"category":"section","location":"applications/thomas/#The-benchmark","page":"Thomas tridiagonal systems","text":"bench/thomas.jl solves 65,536 systems of length 64. Five arrays hold the solution, diagonal, upper diagonal, lower diagonal, and right-hand side. A scratch vector is allocated once and reused. The benchmark also includes a global-SoA alternative that transposes the loops explicitly: row i outside, all systems inside.\n\nThe arithmetic estimate is eight operations per unknown. The LLVM SIMD column is a structural check from code_llvm; it confirms that packet runs contain <P x float>.\n\nconfiguration time estimated GFlop/s speedup vs scalar LLVM SIMD\nscalar reference 29.71 ms 1.1 1.00× —\nglobal SoA rewrite 8.19 ms 4.1 3.63× compiler-controlled\nP=1 29.95 ms 1.1 0.99× —\nP=2 14.88 ms 2.3 2.00× yes\nP=4 7.48 ms 4.5 3.97× yes\nP=8 4.09 ms 8.2 7.27× yes\nP=16 2.18 ms 15.4 13.60× yes\nP=32 1.53 ms 21.9 19.38× yes\n\nP=32 is eight times wider than one native NEON Float32 vector. It wins sequentially because a wide packet gives the processor several independent vector instructions with which to hide division and recurrence latency. P=64 was also tried: it no longer improved this case and its native assembly contained vector stack traffic. Packet size is therefore a measured latency-hiding/unrolling parameter, not a spelling of the register width.","title":"The benchmark"},{"category":"section","location":"applications/thomas/#With-explicit-task-parallelism","page":"Thomas tridiagonal systems","text":"configuration time estimated GFlop/s speedup vs scalar speedup vs threaded P=1\nP=1, 10 threads 4.66 ms 7.2 6.38× 1.00×\nP=2, 10 threads 2.41 ms 13.9 12.34× 1.94×\nP=4, 10 threads 1.32 ms 25.4 22.53× 3.53×\nP=8, 10 threads 0.95 ms 35.2 31.19× 4.89×\nP=16, 10 threads 0.85 ms 39.4 34.88× 5.47×\nP=32, 10 threads 0.89 ms 37.8 33.50× 5.25×\n\nThe packed and threaded gains do not multiply: five arrays stream through memory and the 10-thread run approaches a shared bandwidth limit. This also changes the optimum from sequential P=32 to threaded P=16; an autotuner must include the execution mode in its key.","title":"With explicit task parallelism"},{"category":"section","location":"applications/thomas/#Competing-approaches","page":"Thomas tridiagonal systems","text":"The measured global-SoA rewrite is the most direct competitor: its 3.63× gain proves that a compiler-friendly inner loop across all systems can recover some SIMD. It also requires a different algorithmic source, keeps consecutive recurrence rows nbatch scalars apart, and streams the whole population before advancing one row. DLI completes one small packet of systems at a time, keeps adjacent recurrence states nearby, and reaches 19.38× without transposing the kernel. This is a workload result, not a theorem: global SoA may win when a later stage already consumes that layout or when its long inner loop is especially efficient.\n\nLinearAlgebra.Tridiagonal is the idiomatic first choice for one system and provides a specialised solver. It does not itself express SIMD across thousands of independent systems.\n@simd is not a legal fix for the forward or backward sweep: Julia defines it as a promise that iterations may be reordered. LoopVectorization.@turbo likewise assumed independent iterations. Both are excellent when that premise is true — it simply is not, here.\nA hand-written SIMD.jl kernel can perform the same cross-system vectorization with maximum control. The price is explicit loads, stores, tails, and a second vector-specific kernel. Interleave uses SIMD.jl as its element type while retaining the scalar source.\nParallel cyclic reduction changes the numerical algorithm to expose parallelism within a system. NVIDIA's cuSPARSE batched tridiagonal routines provide strided and interleaved layouts and use CR/PCR on a GPU. This is a stronger option for a large device-resident batch, but it has different hardware, transfer, algorithmic, and floating-point-order trade-offs.","title":"Competing approaches"},{"category":"section","location":"applications/thomas/#Parity-with-Legolas","page":"Thomas tridiagonal systems","text":"The same 4,096-by-64 Thomas workload was compiled from the sibling Legolas++ checkout and run against Julia in interleaved A/B rounds. C++ used -O3, the flags from its benchmark, and -ffp-contract=off; disabling contraction matters because the Julia exactness contract does not silently replace multiply-then-subtract with FMA.\n\npacket Julia Legolas++ Julia / C++ time\n1 1.840 ms 1.755 ms 1.05×\n4 0.459 ms 0.435 ms 1.06×\n8 0.254 ms 0.248 ms 1.03×\n16 0.135 ms 0.134 ms 1.01×\n32 0.0838 ms 0.0833 ms 1.006×\n\nGenerated code explains the close result: both implementations use one NEON group at P=4, two at P=8, four at P=16, and eight at P=32; neither implementation showed vector stack spills through P=32. The audit driver is bench/legolas_cpp_vectorization_audit.cxx. This is encouraging evidence that the Julia representation can match Legolas++, while still falling short of a universal guarantee across kernels, compilers, and CPUs.\n\nChoose Interleave when the Thomas sweep is part of a larger custom CPU kernel and many similarly sized systems are already available together. Choose a domain or vendor routine when its exact primitive, stability behaviour, and data placement match the application better.\n\nFor a complete implementation walkthrough, continue with Tutorial 1.","title":"Parity with Legolas++"},{"category":"section","location":"manual/concepts/#The-mental-model","page":"Mental model","text":"Interleave is easier to understand if three different kinds of parallelism remain separate.\n\nLevel Unit of work Interleave API\nrecurrence the next position inside one problem your kernel loop\nSIMD the same position in several independent problems Vec{P,T} elements\ntasks groups of SIMD packets parallel_apply!\n\nThe first level is sequential. The other two exploit independence around it.\n\nOn a GPU the mapping changes, not the algorithm: one work item owns one complete recurrence and neighbouring work items span the independent population. See GPU execution: recurrence per work item for the shared KernelAbstractions implementation and its Metal, CUDA, and AMDGPU targets.","title":"The mental model"},{"category":"section","location":"manual/concepts/#Data-Layout-Interleaving","page":"Mental model","text":"Suppose each problem owns a vector of length n, and there are nbatch problems. The logical user view is:\n\n(nbatch, n) scalars\n\nWith packet size P, Interleave stores:\n\n(n, cld(nbatch, P)) values of type Vec{P,T}\n\nEach packed value contains one position from P problems. A kernel walks a dense vector of these values. Its dependency remains sequential along n; its arithmetic is SIMD across the batch.\n\n(Image: Data Layout Interleaving in memory)\n\nThis arrangement is often called an array of structures of arrays (AoSoA). “Data Layout Interleaving” emphasizes why it is used here: independent recurrence chains are interleaved just enough to form vector packets.","title":"Data Layout Interleaving"},{"category":"section","location":"manual/concepts/#Why-packed-storage-is-native","page":"Mental model","text":"The hot parent is directly a dense Base.Array{Vec{P,T}}; the multidimensional Interleave.Array wrapper supplies the logical scalar view. In other words, the implementation uses the second of two plausible designs: store vector blocks natively, then wrap them as a scalar batch. It does not keep a scalar parent and reinterpret it into vectors on every kernel access.\n\nThat distinction is measurable in Julia. On the same Thomas P=16 experiment, native packed storage took 2.17 ms. A raw-pointer view over scalar storage took 2.79 ms, reinterpret indexing 3.44 ms, vload 4.92 ms, and a manual lane loop 9.77 ms. The bytes were equivalent; the optimizer's view of indexing was not. parent(A) therefore exposes the native packed array used by the kernel, while scalar indexing remains a setup, I/O, and validation path.","title":"Why packed storage is native"},{"category":"section","location":"manual/concepts/#What-“vectorized-by-construction”-guarantees","page":"Mental model","text":"Changing an element to Vec{P,T} makes supported arithmetic explicitly vector-valued before LLVM's loop-vectorization heuristics run. This is a much stronger structural condition than hoping the compiler recognizes independent recurrence iterations. It is not, however, an absolute promise about every final instruction: LLVM may split a wide packet into native registers, scalarize an unsupported operation, introduce a helper call, or spill under register pressure.\n\nInterleave therefore treats “pure SIMD” as an auditable contract for each kernel and target:\n\nexact scalar-to-packed tests establish semantic equivalence;\ntyped LLVM must contain the expected vector values;\nnative assembly must show vector arithmetic and no harmful hot-loop spills;\nBenchmarkTools must confirm that the structure improves runtime.\n\nThomas passes that audit through P=32 on the measured M1 and closely matches Legolas++. At P=64, vector stack traffic appears, illustrating why the last two checks cannot be replaced by a type-level theorem.","title":"What “vectorized by construction” guarantees"},{"category":"section","location":"manual/concepts/#One-kernel,-two-meanings-of-an-element","page":"Mental model","text":"The central abstraction is not a macro or a compiler pass. It is the element type:\n\npacktype(Float32, Val(1)) === Float32\npacktype(Float32, Val(8)) === Vec{8,Float32}\n\nIf a kernel uses only operations defined for both types, Julia specializes the same source for scalar and packed inputs. Local variables derived from array elements become packed automatically.\n\nThis gives Interleave an unusual but useful separation:\n\nthe algorithm decides dependency order;\nthe container type decides the SIMD axis;\nthe driver name decides whether tasks are launched.","title":"One kernel, two meanings of an element"},{"category":"section","location":"manual/concepts/#Why-this-is-not-ordinary-auto-vectorization","page":"Mental model","text":"Auto-vectorization normally looks for independent iterations in a loop. A recurrence tells the compiler those iterations are dependent, so it must preserve their order. Interleave does not ask the compiler to disprove that dependency. It presents several independent values as one explicit SIMD element.\n\nNor is this merely multithreading. Threads operate on chunks of packets and have much higher granularity. SIMD happens inside one core and is available through plain apply!.","title":"Why this is not ordinary auto-vectorization"},{"category":"section","location":"manual/concepts/#What-Interleave-deliberately-does-not-do","page":"Mental model","text":"Interleave does not:\n\ndiscover whether two problems are independent;\nconvert arbitrary control flow into masks;\nchoose the best packet size automatically;\nmake a kernel faster merely because it is packed;\nhide task creation inside a supposedly sequential call;\nport the recursive shape system or expression-template machinery of Legolas++.\n\nJulia already supplies multidimensional arrays, views, specialization, and dispatch. The port keeps the DLI programming idea and discards infrastructure that existed mainly to work around C++ constraints.","title":"What Interleave deliberately does not do"},{"category":"section","location":"manual/concepts/#The-synchronization-rule","page":"Mental model","text":"Lanes should execute the same control-flow shape. Lane-varying arithmetic is fine; a lane that needs a different number of iterations or exits early is a poor match. Horizontal operations that combine lanes also change the meaning of the batch and should remain outside the kernel unless they are explicitly part of the algorithm.","title":"The synchronization rule"},{"category":"section","location":"manual/what-it-replaces/#What-you-would-write-instead","page":"What you would write instead","text":"Positioning and alternatives surveys the tools. This page answers the narrower and more useful question, kernel by kernel: if Interleave did not exist, what would you actually write, and would you be worse off?\n\nTwo of the six answers are \"you would be better off\". Those are the important ones. Where a competing package exists, it is measured rather than characterised — see the DSP.jl table below, which corrects an earlier unmeasured claim on this page.\n\nEvery table here is reproduced by a file in bench/. julia --project=bench bench/studies.jl runs the whole set at reduced sizes in about 90 seconds and reaches the same conclusions; each file's own defaults reproduce the exact numbers printed here.","title":"What you would write instead"},{"category":"section","location":"manual/what-it-replaces/#Where-the-numbers-come-from","page":"What you would write instead","text":"Everything below was measured by bench/runall.jl on an Apple M-series machine (NEON, 128-bit vectors, 8 threads, Float32). The reference is the naive Julia loop over a Base.Array batch — the code you would write first, with no annotation.\n\nkernel recurrence? reference throughput best DLI with threads\nThomas tridiagonal yes 1.1 GFlop/s P=16: 13.4× 31.8×\nIIR biquad yes 3.0 GFlop/s P=16: 15.0× 32.9×\nBlack–Scholes CN yes (double) 1.9 GFlop/s P=16: 11.8× 84.6×\nDepthwise 3×3 no 40.6 GFlop/s P=16: 0.43× 1.73×\nSobel + motion no 43.4 GFlop/s P=8: 0.88× 2.77×\n\ntip: Two questions decide, and neither is the application domain\n1. What throughput does your reference reach? At 1–3 GFlop/s the compiler has failed — there is a recurrence, and DLI returns 12–15×. At 40+ GFlop/s LLVM already vectorized the inner loop; DLI can then only take over that gain, never add to it, and it takes it over badly.2. How many predecessors does the recurrence carry? This decides how DLI compares to the serious alternative, a hand-written SoA layout. One predecessor and SoA wins; beyond that DLI pulls ahead and keeps pulling — 2.2× on a tridiagonal solve, 11.8× on a pentadiagonal one, 8.4× on a 16th-order IIR. The sweeps are below.The two questions are independent, and the second can overturn the first: adding a recursive filter to the Sobel pipeline — a documented loss at 0.69× — turns it into a 3.6× win without touching the stencil.The mechanism is worth stating plainly: Vec{P,T} as an element type blocks the auto-vectorization LLVM would have performed by itself. On the two stencil kernels, P=1 is 4–10× faster than P=16, because a one-lane vector element prevents vectorization along the contiguous axis. On the recurrence kernels, P=1 tracks the reference to within 2% — there was nothing to block.","title":"Where the numbers come from"},{"category":"section","location":"manual/what-it-replaces/#1.-Thomas-tridiagonal-solve-—-Interleave-wins","page":"What you would write instead","text":"Without Interleave you would loop over systems and call the scalar sweep, or reach for LinearAlgebra.Tridiagonal. Neither helps:\n\nTridiagonal plus ldiv! dispatches to LAPACK's gtsv one system at a time. For 65 536 systems of length 64, per-call overhead dominates and there is no batching.\n@simd on the forward sweep is simply illegal: X[i] depends on X[i-1]. The performance tips warn that the macro promises reorderable iterations and produces wrong results otherwise.\nLoopVectorization.@turbo assumed iteration independence, so it was illegal on the same axis for the same reason — and it is in any case no longer a live option here, being maintenance-only and falling back to @inbounds @fastmath on Julia ≥ 1.11, which breaks the bit-exactness this package guarantees.\n\nThe one alternative that does work is to transpose your data into a global SoA layout and rewrite the kernel as \"time outside, instances inside\", with @simd on the instance loop. That is legal — instances are independent — and it is DLI done by hand. It is measured below.\n\nVerdict. 13.4× at P=16, and tune finds 18.3× at P=32 on this machine — the packet size is genuinely worth measuring rather than assuming.\n\nStrength: the kernel source is unchanged and stays bit-exact against its scalar self. Weakness: you own a tuning parameter you did not have before.","title":"1. Thomas tridiagonal solve — Interleave wins"},{"category":"section","location":"manual/what-it-replaces/#DLI-against-a-hand-written-global-SoA","page":"What you would write instead","text":"Transposing to global SoA is the serious alternative, so it deserves a measurement rather than an assertion. bench/soavsdli.jl runs three traversals over the same (nbatch, nx) array — identical footprint, only the loop order differs:\n\nreference: b outside, i inside — strided along the recurrence;\nSoA: i outside, b inside with @simd — instances contiguous;\nDLI: one packet crosses the whole recurrence before the next starts.\n\nAll three agree bit-exactly. Thomas, nx = 64, P = 16, Float32, one core of an Apple M1 Max (L1d 128 KB, L2 12 MB):\n\nnbatch SoA working set per i step reference SoA @simd DLI DLI / SoA\n256 7 KB 0.15 ms 0.01 ms 0.01 ms 1.38×\n1 024 28 KB 0.83 ms 0.08 ms 0.03 ms 2.26×\n16 384 448 KB 25.60 ms 1.08 ms 0.55 ms 1.98×\n262 144 7.0 MB 700.84 ms 20.24 ms 9.87 ms 2.05×\n524 288 14.0 MB 1065.75 ms 43.61 ms 18.82 ms 2.32×\n1 048 576 28.0 MB 2369.29 ms 80.81 ms 37.44 ms 2.16×\n2 097 152 56.0 MB 6063.74 ms 165.68 ms 74.36 ms 2.23×\n\nDLI beats hand-written SoA by a steady factor of about 2.2 on this kernel, and the ratio does not grow with the population. The sweep deliberately crosses the 12 MB L2 boundary — from 7 KB to 56 MB, 4.6× past it — with no visible change.\n\nOn this kernel the mechanism is memory traffic rather than cache behaviour. Both variants are bandwidth-bound and reach comparable bandwidth (36 versus 29 GB/s single-threaded); SoA simply moves about 1.8× more bytes, because back substitution needs one scratch value per instance per step, so the SoA formulation must materialise an entire nbatch × nx workspace in DRAM while the DLI workspace is nx × P and never leaves L1.","title":"DLI against a hand-written global SoA"},{"category":"section","location":"manual/what-it-replaces/#The-tridiagonal-result-does-not-generalise","page":"What you would write instead","text":"It would be easy to stop there and conclude that the DLI advantage is a modest constant. That conclusion is wrong, and bench/pentasoa.jl shows why. A pentadiagonal solve needs two auxiliary sequences carried to the back substitution instead of one, so the SoA form materialises 2 × nbatch × n of workspace and reaches back two columns in several arrays at once. Same three traversals, same bit-exact agreement, n = 64, P = 16:\n\nnbatch SoA scratch reference SoA @simd DLI DLI / SoA\n16 384 8 MB 34.96 ms 10.46 ms 1.14 ms 9.19×\n262 144 128 MB 886.40 ms 200.16 ms 17.73 ms 11.29×\n1 048 576 512 MB 4046.66 ms 870.45 ms 76.77 ms 11.34×\n2 097 152 1024 MB 8186.06 ms 1734.67 ms 147.49 ms 11.76×\n\nThe gap goes from 2.2× to nearly 12×, and a traffic-only model does not explain it — traffic differs by just 1.57×. The achieved bandwidth is where it happens:\n\npentadiagonal, nbatch = 2 097 152 bytes moved achieved\nDLI 3.76 GB 25.5 GB/s\nSoA 5.91 GB 3.4 GB/s\n\nSoA's achieved bandwidth collapses by 7.5×, and 1.57 × 7.5 ≈ 11.8 accounts for the measurement. The cause is the number of simultaneous batch-major streams: roughly 7 for the tridiagonal form, 11 to 13 for the pentadiagonal one, each striding nbatch × 4 bytes — 8 MB at this size — between consecutive i. That exceeds what the hardware prefetchers and the TLB sustain, and the memory system falls off a cliff. DLI is immune by construction: its entire per-packet working set is n × P per array, about 37 KB in total here, whatever nbatch is and however many bands the matrix has.\n\nSo the locality argument for AoSoA is real after all — it simply needs enough concurrent streams to appear. The tridiagonal kernel sits below that threshold and shows only the traffic effect; the pentadiagonal kernel sits above it. The honest summary is that the DLI advantage over hand-written SoA grows with the number of arrays the recurrence must carry, not with the size of the population.","title":"The tridiagonal result does not generalise"},{"category":"section","location":"manual/what-it-replaces/#It-is-the-number-of-predecessors-that-decides","page":"What you would write instead","text":"Tridiagonal gave 2.2×, pentadiagonal 11.8×. Those are two points on a curve, and the curve has a parameter: how many predecessors the recurrence carries. bench/orderscan.jl makes it continuous with an order-M IIR,\n\nyn = b_0 xn + sum_k=1^M b_k xn-k - sum_k=1^M a_k yn-k\n\nwhere the SoA form must keep 2M+1 concurrent batch-major streams while DLI holds 2M state values in registers. 65 536 channels × 1024 samples, P = 16, all three variants bit-exact:\n\nM SoA streams reference SoA @simd DLI DLI / SoA\n1 3 4393 ms 12.74 ms 31.51 ms 0.40×\n2 5 5148 ms 229.29 ms 43.96 ms 5.22×\n4 9 793 ms 342.34 ms 79.01 ms 4.33×\n8 17 2436 ms 1165.68 ms 146.71 ms 7.95×\n16 33 7245 ms 2700.83 ms 321.75 ms 8.39×\n\nReproducible to about 2% across runs. Two things to take from it.\n\nThere is a floor, and it matters. At M = 1 — three streams — hand-written SoA beats DLI by 2.5×. A first-order recurrence is the ideal case for the SoA form: few streams, perfectly vectorized, no workspace. The advantage of DLI is not universal in the order either.\n\nAbove that floor the advantage grows with M, from 5.2× to 8.4×, with a dip at M = 4 that breaks strict monotonicity. The reference column is reproducibly non-monotonic too and is not the object of this study; it is reported rather than explained.","title":"It is the number of predecessors that decides"},{"category":"section","location":"manual/what-it-replaces/#The-same-effect-on-a-2-D-kernel,-where-it-flips-the-verdict","page":"What you would write instead","text":"The Sobel-plus-motion pipeline is the documented loss: a pure stencil, already vectorized. bench/videorec.jl prepends what a real image pipeline does — a recursive smoothing along rows, Deriche/van Vliet family, of order M — and keeps the Sobel. M = 0 is the original kernel. The reference is the strong one: (H, W, nstream) arrays where each image is contiguous, exactly as bench/video.jl uses. 256 streams of 128×128, P = 8, bit-exact:\n\nM reference DLI DLI / reference\n0 4.33 ms 6.29 ms 0.69×\n1 8.40 ms 6.27 ms 1.34×\n2 12.70 ms 6.82 ms 1.86×\n4 23.66 ms 7.67 ms 3.09×\n8 39.59 ms 11.05 ms 3.58×\n\nThe verdict flips between M = 0 and M = 1 and then grows. The mechanism is visible in the columns rather than the ratio: DLI's time rises by 1.8× across the sweep while the reference's rises by 9.1×. DLI does not get faster — the alternatives get slower, because a row-scan recurrence is exactly what the compiler cannot vectorize, and DLI absorbs it into registers.\n\n(M = 0 reads 0.69× here against 0.88× in the summary table: this kernel writes through an extra smoothing buffer that the original does not, which costs both variants equally but is not the same measurement.)","title":"The same effect on a 2-D kernel, where it flips the verdict"},{"category":"section","location":"manual/what-it-replaces/#And-the-SoA-source-is-genuinely-harder-to-get-right","page":"What you would write instead","text":"This is usually argued as a matter of taste. It is not only that. Writing these benchmarks, the DLI variant was in every case the scalar kernel, unchanged — that is the whole premise, and it cannot drift from the reference because it is the reference.\n\nEach SoA variant, by contrast, needed a second implementation: a transposed loop nest, an explicit boot phase for the first M samples, and separate batch-sized workspaces. The boot phase is where a real bug appeared here — it accumulated acc + (b·x - a·y) where the generated steady-state expression computes (acc + b·x) - a·y. Different associativity, one ulp in Float32, and bit-exactness with the scalar reference was silently lost. It looked like a property of SoA until it was tracked down; it was a typo in a loop that exists only because the layout changed.\n\nThat is the ergonomic argument made concrete: the alternative is a second source of truth, and second sources drift.\n\nnote: Four kernels, one machine\nTridiagonal, pentadiagonal, an order-M IIR and a recursive video filter, on an M1 Max. The two differ by more than 5×, which is the point: a kernel whose SoA form needs no batch-sized workspace would narrow the gap further still, and a machine with different prefetcher or TLB limits would move the threshold. Re-run bench/soavsdli.jl and bench/pentasoa.jl rather than porting a verdict.","title":"And the SoA source is genuinely harder to get right"},{"category":"section","location":"manual/what-it-replaces/#2.-IIR-biquad-filter-bank-—-measured-against-DSP.jl","page":"What you would write instead","text":"Without Interleave you would use DSP.jl and filt. filt(b, a, X) on a matrix filters each column independently, so a filter bank is one call — it is the direct competitor, not an approximation of one.\n\nAn earlier revision of this page asserted that DSP.jl was \"a real, well-optimized answer\" without measuring it. It is worth separating the two claims, because only one survives.\n\nbench/dspcompare.jl, 1024 independent channels × 4096 samples, Float32, same coefficients, each library in its own natural layout (DSP filters columns of (ns, nb), Interleave works batch-major (nb, ns)), numerical agreement checked before timing:\n\nvariant time vs naive loop vs DSP.filt!\nnaive Julia loop 27.321 ms 1.00× 0.82×\nDSP.filt 22.545 ms 1.21× 0.99×\nDSP.filt! (in place) 22.334 ms 1.22× 1.00×\nInterleave P=1 13.001 ms 2.10× 1.72×\nInterleave P=8 1.622 ms 16.84× 13.77×\nInterleave P=16 0.820 ms 33.31× 27.23×\nInterleave P=32 1.070 ms 25.54× 20.88×\n\nOn throughput, DSP.jl loses by a factor of 27. That is not a criticism of DSP.jl: filt runs the same strict temporal recurrence per channel that no compiler vectorizes, so it beats a naive loop by only 1.22×. The recurrence is the wall, and DSP.jl does not try to go around it by batching across channels.\n\nTwo details in that table are worth more than the headline:\n\nP=1 already wins 2.10× with no SIMD at all. An interleaved instance is contiguous, while X[c, n] in the naive column-major loop strides across channels. Part of the gain is pure locality, not vectorization.\nP=32 is worse than P=16 here, where the opposite is true for Thomas. The optimum is genuinely per-kernel, which is why tune exists.\n\nInterleave computes bit-exactly the same result as the naive loop (max difference exactly 0.0); DSP.jl differs by 2.4e-7, having its own accumulation order.\n\nVerdict. DSP.jl wins decisively on features — filter design (Butterworth, Chebyshev), form conversion, stability analysis — none of which Interleave has or should have. It loses on throughput for a bank by 27×. The honest recommendation is unchanged in shape but sharper in reason: design the filter with DSP.jl, run the bank with Interleave.","title":"2. IIR biquad filter bank — measured against DSP.jl"},{"category":"section","location":"manual/what-it-replaces/#3.-Black–Scholes-Crank–Nicolson-—-the-best-case,-at-a-price","page":"What you would write instead","text":"Without Interleave you would either hand-roll the scheme (what the benchmark's reference does) or use MethodOfLines.jl and the SciML stack.\n\nSciML buys adaptivity, event handling, sensitivity analysis, and a solver ecosystem. Interleave buys none of that. But once you have committed to a fixed scheme — Crank–Nicolson, fixed grid, fixed number of steps — that machinery is overhead on a kernel that is now just two nested recurrences.\n\nVerdict. 11.8× sequential and 84.6× with threads — the best result in the suite, because the double recurrence (time × sweep) is doubly hostile to auto-vectorization, and the working set stays small enough that threads still scale.\n\nWeakness: you have hand-written a numerical scheme and inherited the duty to validate it.","title":"3. Black–Scholes Crank–Nicolson — the best case, at a price"},{"category":"section","location":"manual/what-it-replaces/#4.-Depthwise-33-convolution-—-Interleave-loses","page":"What you would write instead","text":"This is the boundary case, and it is in the suite on purpose.\n\nThe plain Julia loop already reaches 40.6 GFlop/s: LLVM auto-vectorizes along the contiguous axis without being asked. DLI at P=16 gives 0.43× — that is 2.3× slower than doing nothing.\n\nWhat you should use instead: NNlib's depthwiseconv for the standard primitive, or Tullio.jl in index notation for a custom variant. Both are designed for exactly this shape and both are maintained. (LoopVectorization.@turbo was the third suggestion here; see Positioning and alternatives for why it was withdrawn.)\n\nWhat Interleave offers here: set P = 1 and the same kernel returns to parity (1.05×). That is the useful property — the escape hatch is one character, not a rewrite.","title":"4. Depthwise 3×3 convolution — Interleave loses"},{"category":"section","location":"manual/what-it-replaces/#5.-Sobel-motion-—-Interleave-loses-again","page":"What you would write instead","text":"Same story: a 43.4 GFlop/s reference, P=8 gives 0.88×, and P=1 restores parity.\n\nInstead: Tullio.jl, or ImageFiltering.jl for standard imfilter-shaped work.\n\nThese two negative cases are the reason the package documents a decision rule rather than a speedup claim.","title":"5. Sobel + motion — Interleave loses again"},{"category":"section","location":"manual/what-it-replaces/#The-same-algorithm-wins-in-C-—-and-that-is-not-a-contradiction","page":"What you would write instead","text":"Legolas++, the C++ project these ideas come from, measured the same Sobel-plus-temporal pipeline and found DLI winning:\n\nSobel + temporal, 32×720p (C++, AVX2, Ryzen 5 3600, GCC 15.2 -O3 -march=native) time vs scalar\nCPU scalar, 1 core 56.17 ms 1.00×\nCPU DLI AVX2, 1 core 19.95 ms 2.82×\n\n2.82× there, 0.88× here, on the same algorithm. The explanation is not that DLI behaves differently — it is that the two verdicts are measured against different baselines:\n\n baseline throughput DLI verdict\nC++ video pipeline 12.1 GFlop/s 2.82×\nC++ depthwise 49.2 GFlop/s 1.09×\nJulia Sobel + motion 43.4 GFlop/s 0.88×\nJulia depthwise 40.6 GFlop/s 0.43× (P=1: 1.05×)\n\nRead the first column and the second follows. GCC did not vectorize the C++ video kernel — 12 GFlop/s is a scalar baseline — so DLI recovered what the compiler had left on the table. GCC did vectorize the C++ depthwise kernel, and DLI gained nothing there either (1.09×). LLVM vectorized both Julia kernels, so DLI has nothing left to recover in either.\n\nSo the decision rule does not merely survive the cross-language comparison; it predicts it. What changes between the two projects is not the technique but which kernels the compiler happened to handle.\n\nThe practical consequence is a warning: a DLI verdict is not portable. It is a property of your kernel, your compiler and your machine together, and a 2.82× measured in C++ with GCC on AVX2 tells you nothing about the same algorithm in Julia with LLVM on NEON. Measure the reference throughput on the machine you will actually run on.\n\nnote: This is a comparison of published numbers, not a controlled experiment\nThe two campaigns differ in machine (Ryzen 5 3600 / AVX2 versus Apple M-series / NEON), in problem size (32×720p ≈ 29.5 Mpixels and 118 MB per buffer, against 256×128² ≈ 4.2 Mpixels and about 16 MB here), and therefore in memory regime. The baseline throughputs are directly comparable; the speedups should be read as evidence for the mechanism, not as a head-to-head between the two implementations.","title":"The same algorithm wins in C++ — and that is not a contradiction"},{"category":"section","location":"manual/what-it-replaces/#6.-Per-instance-reductions","page":"What you would write instead","text":"Covered by bench/reductions.jl. A squared norm or dot product per instance has no recurrence, so Base is already competitive: sum(abs2, A; dims=2) reduces along a contiguous axis and vectorizes well.\n\nDLI still has a structural argument here — the reduction is expressed once, scalar, and runs inside the same batch traversal as the recurrence kernels that surround it, avoiding a layout round-trip. Measure before assuming a gain.\n\n","title":"6. Per-instance reductions"},{"category":"section","location":"manual/what-it-replaces/#Strengths-of-the-approach","page":"What you would write instead","text":"One kernel, one source. The scalar kernel is the vectorized kernel. There is no second implementation to keep in sync, and the central test asserts bit-exact (==, not ≈) agreement between the two layouts.\nThe gain lands where compilers fail. 12–15× on recurrences is not an incremental win; it is the difference between a batch being feasible and not.\nA one-character escape hatch. P = 1 returns control to the compiler and makes the container an ordinary dense array. A kernel that turns out not to suit DLI costs nothing to leave in place.\nParallelism stays visible. apply! cannot spawn tasks; only parallel_apply! does. A caller already inside a parallel region is never surprised.\nThe same source reaches the GPU. gpu_apply! runs the identical scalar kernel with one work item per problem — no packets, no second language.","title":"Strengths of the approach"},{"category":"section","location":"manual/what-it-replaces/#Weaknesses,-stated-plainly","page":"What you would write instead","text":"P must be measured. There is no universally good default: the best value is neither the hardware vector width nor a constant across kernels. On this machine P=32 beats P=16 on Thomas, four times the 128-bit NEON width, because a latency-bound recurrence wants several vectors in flight. tune exists to answer this, but it is still a measurement you must make.\nIt actively hurts already-vectorized kernels. Not \"no gain\" — a real 2.3× loss if you leave P>1 on a stencil. The decision rule is not optional.\nNo domain features. No filter design, no adaptive time-stepping, no automatic differentiation, no autodiff-through-the-solver. Domain libraries keep their value, and the right answer is often to combine them.\nThe batch must be large and homogeneous. P problems advance in lockstep. Ragged lengths, early exits, or data-dependent branching per instance break the model.\nElement types are restricted. Vec{P,T} requires an LLVM leaf type, so Vec{P,Dual} does not compose: the genericity holds on each axis separately, not on their product.\nThreading gains are not multiplicative with P. Once P is well chosen the kernel is memory-bandwidth-bound; Thomas goes from 13.4× to only 16.2× on 8 threads. Do not sell the two gains as a product.","title":"Weaknesses, stated plainly"},{"category":"section","location":"manual/what-it-replaces/#The-one-line-summary","page":"What you would write instead","text":"Interleave is worth reaching for when the kernel is custom, recurrence-heavy, and embedded in a larger CPU workflow. It loses to the compiler when the loop was already vectorizable, and it loses to a domain library when that library already implements your exact primitive. Measure the reference throughput first; it tells you which case you are in.","title":"The one-line summary"},{"category":"section","location":"manual/alternatives/#Positioning-and-alternatives","page":"Positioning and alternatives","text":"Interleave is neither a new numerical algorithm nor a replacement for Julia's compiler. It is a narrow programming model for one awkward case: many independent copies of a loop-carried recurrence on a CPU.","title":"Positioning and alternatives"},{"category":"section","location":"manual/alternatives/#The-decision-table","page":"Positioning and alternatives","text":"approach best at recurrence inside one instance source impact relationship to Interleave\nJulia/LLVM auto-vectorization regular contiguous loops usually blocked none always try first\n@simd loops known to have independent iterations illegal for a true recurrence one annotation and a correctness promise alternative only on another legal axis\nTullio tensor contractions, convolutions, stencils not a general feedback-recurrence language rewrite in index notation the maintained choice for depthwise/Sobel-like kernels\nLoopVectorization the same, historically assumes iteration independence macro around the loop maintenance-only; see the warning below\nTullio tensor contractions, convolutions, stencils not a general feedback-recurrence language rewrite in index notation domain-oriented alternative for regular array algebra\nexplicit SIMD.jl complete manual control works if lanes are independent instances vector loads, stores, tails, and packet-aware code low-level foundation used by Interleave\ntask parallelism coarse independent jobs yes, one recurrence per task parallel driver/scheduler complementary; Interleave keeps it explicit\ndomain/vendor library a standard primitive it already implements library-specific adapt data and API often the production choice when it matches\nGPU kernel/library very large device-resident parallel workloads map instances to threads or change algorithm device code/data management different scale and hardware target\n\nnote: A second axis, measured\nThe table above sorts by what the tool is good at. A second question sorts the outcome just as strongly: how many predecessors the recurrence carries. A hand-written SoA layout beats DLI on a first-order recurrence and loses by 8× on a sixteenth-order one, and a kernel with no recurrence at all is where DLI loses outright. See What you would write instead for the sweeps.","title":"The decision table"},{"category":"section","location":"manual/alternatives/#What-the-compiler-can-and-cannot-infer","page":"Positioning and alternatives","text":"Julia often lets LLVM vectorize straightforward loops automatically. The official performance tips warn that @simd promises reorderable iterations and may produce wrong results when dependencies are present. A Thomas forward sweep or IIR feedback loop has exactly such a dependency.\n\nInterleave does not ask the compiler to disprove it. It changes the element from Float32 to Vec{P,Float32} so each ordinary arithmetic expression already denotes P independent operations. The sequential loop remains sequential.","title":"What the compiler can and cannot infer"},{"category":"section","location":"manual/alternatives/#Loop-transformation-tools","page":"Positioning and alternatives","text":"Tullio.jl expresses convolutions, stencils, broadcasts, and reductions in index notation and can cooperate with threading. It gives up the \"keep an arbitrary scalar imperative kernel\" goal in exchange for a much richer optimiser view of regular tensor algebra. It is actively maintained and is the tool to reach for on the kernels where Interleave declines.\n\nwarning: LoopVectorization is no longer the answer here\nEarlier revisions of this documentation recommended LoopVectorization.@turbo for stencil-shaped kernels. That recommendation is withdrawn, for two reasons.It is maintained through the SciML Small Grants programme rather than actively developed, and active support for Julia ≥ 1.11 has been dropped: on those versions @turbo simply runs @inbounds @fastmath instead.That fallback is specifically wrong for this project. @fastmath licenses FMA contraction, which changes the order of operations and breaks the bit-exact scalar ↔ vectorized agreement that is invariant 1 here. A tool that silently applies the one thing the package forbids cannot be recommended alongside it, whatever its merits on Julia 1.10.\n\nThese tools are not universally competing. A real application can use Tullio for an explicit stencil stage and Interleave for the following implicit solve.","title":"Loop transformation tools"},{"category":"section","location":"manual/alternatives/#Explicit-vector-programming","page":"Positioning and alternatives","text":"SIMD.jl exposes vector types and operations directly. It makes legality explicit and is the foundation of Vec{P,T} in Interleave. Used alone, it also requires the programmer to arrange loads, stores, tails, and the mapping between lanes and instances. That is appropriate when every instruction matters or when the operation cannot be expressed generically.\n\nInterleave contributes the missing policy layer: a logical scalar batch, dense packet storage, instance views, padding rules, scratch ownership, and sequential or explicitly parallel drivers. The kernel remains ordinary Julia source.\n\nStructArrays.jl solves a different layout problem: it presents an array of structures while storing one array per field. It can be complementary for structured model parameters, but it does not select a SIMD batch axis or drive recurrence kernels.","title":"Explicit vector programming"},{"category":"section","location":"manual/alternatives/#Threads-and-GPUs","page":"Positioning and alternatives","text":"Low-overhead task schedulers such as OhMyThreads or Polyester.@batch distribute independent jobs across CPU cores. Interleave deliberately separates this choice: apply! never launches tasks, and parallel_apply! makes scheduling visible. This avoids hidden nested parallel regions and lets SIMD and thread scaling be measured independently.\n\nCUDA.jl offers both array programming and custom Julia kernels on NVIDIA GPUs. Vendor libraries can go further for standard operations; for example, cuSPARSE implements batched tridiagonal solvers, including an interleaved layout. GPUs are strong when the population is large and already device-resident. Interleave instead targets composable CPU kernels, small or medium working sets, and applications where changing numerical source is undesirable.","title":"Threads and GPUs"},{"category":"section","location":"manual/alternatives/#Domain-libraries","page":"Positioning and alternatives","text":"Use a specialised library when the workload is already one of its primitives:\n\nLinearAlgebra.Tridiagonal for idiomatic tridiagonal matrices and specialised solves;\nDSP.jl for filter design, filter forms, and stateful signal processing;\nNNlib for depthwise convolution, differentiation rules, and CPU/GPU neural-network integration;\nMethodOfLines.jl for symbolic PDE discretisation and SciML solver composition.\n\nThe value of Interleave rises when the kernel is custom, recurrence-heavy, and embedded in a larger CPU workflow. It falls when a mature library already implements the exact operation.","title":"Domain libraries"},{"category":"section","location":"manual/alternatives/#What-is-genuinely-distinctive","page":"Positioning and alternatives","text":"Data interleaving and cross-instance SIMD are established ideas. The distinctive part of Interleave.jl is their Julia interface and its constraint: the scalar kernel should remain the only kernel. A normal Base.Array is the scalar configuration; a Interleave.Array changes the element type presented to the same code; P=1 cleanly returns control to ordinary compiler vectorization.\n\nThat constraint produces a useful form of performance portability across algorithms, not a promise that every kernel gets faster. The two negative benchmark cases are essential evidence for that boundary.","title":"What is genuinely distinctive"},{"category":"section","location":"applications/depthwise/#Depthwise-33-convolution","page":"Depthwise 3×3 convolution","text":"Depthwise convolution applies one spatial filter per channel. Different output pixels are independent once the input is fixed, so the ordinary contiguous inner loop is already an excellent SIMD target.\n\n(Image: Spatial SIMD compared with DLI across channels)\n\nThis benchmark is intentionally adversarial to Interleave. The left side of the animation is what LLVM sees in the reference: adjacent output pixels can advance together. DLI chooses the same coordinate in several channels instead. That mapping is legal, but it replaces an already successful SIMD direction rather than unlocking a blocked one.","title":"Depthwise 3×3 convolution"},{"category":"section","location":"applications/depthwise/#The-benchmark","page":"Depthwise 3×3 convolution","text":"bench/depthwise.jl applies one 3×3 Float32 stencil to 512 independent 64×64 feature maps. The estimate is nine multiplies and eight additions for every interior pixel.\n\nconfiguration time estimated GFlop/s speedup vs scalar LLVM SIMD\nscalar reference 0.81 ms 41.1 1.00× compiler-controlled\nP=1 0.79 ms 42.5 1.03× compiler-controlled\nP=2 4.75 ms 7.0 0.17× yes, across channels\nP=4 3.02 ms 11.1 0.27× yes, across channels\nP=8 2.49 ms 13.4 0.33× yes, across channels\nP=16 1.94 ms 17.2 0.42× yes, across channels\nP=32 1.61 ms 20.8 0.51× yes, across channels\n\nThe presence of vector instructions at P>1 is not evidence of a good transformation. Even P=32, the least slow packed case, takes about twice the reference time. P=1 is deliberately the scalar type Float32, not Vec{1,Float32}, so LLVM recovers its normal spatial strategy and performance returns to parity.","title":"The benchmark"},{"category":"section","location":"applications/depthwise/#With-explicit-task-parallelism","page":"Depthwise 3×3 convolution","text":"configuration time estimated GFlop/s speedup vs scalar speedup vs threaded P=1\nP=1, 10 threads 0.39 ms 85.7 2.08× 1.00×\nP=2, 10 threads 1.02 ms 32.9 0.80× 0.38×\nP=4, 10 threads 0.69 ms 48.2 1.17× 0.56×\nP=8, 10 threads 0.56 ms 60.2 1.46× 0.70×\nP=16, 10 threads 0.50 ms 66.6 1.62× 0.78×\nP=32, 10 threads 0.40 ms 83.7 2.04× 0.98×\n\nThreading helps because channels are independent, but the best result keeps P=1. Comparing only P=32 threaded with the serial reference would misleadingly attribute a 2.04× result to DLI; compared with threaded P=1, the packed layout is still slightly slower.","title":"With explicit task parallelism"},{"category":"section","location":"applications/depthwise/#Competing-approaches","page":"Depthwise 3×3 convolution","text":"Plain Julia and LLVM are already the relevant competitor. @simd may help only when its independence promise is valid and aliasing is understood; it should never be added by habit.\nTullio.jl expresses convolutions and stencils in index notation and combines loop transformation, tiling, and threading. It attacks the spatial loop — the right axis for this workload — and it is maintained. LoopVectorization.@turbo did the same and was the earlier recommendation here; it is now maintenance-only and falls back to @inbounds @fastmath on Julia ≥ 1.11, so it is no longer suggested alongside a package whose central invariant forbids @fastmath.\nNNlib.depthwiseconv is the production-oriented choice in a neural-network pipeline. NNlib also has CUDA and AMDGPU extensions and ChainRules support.\nHand-written explicit SIMD offers control but is unnecessary unless inspection proves that the compiler missed the regular spatial loop.\n\nThe lesson is a feature, not an embarrassment: one kernel can remain in the Interleave workflow while P=1 opts out of DLI. This control case prevents the project from claiming speedups against an artificially scalar reference.","title":"Competing approaches"},{"category":"section","location":"applications/video/#Fused-Sobel-and-motion-detection","page":"Sobel plus motion","text":"This kernel combines a 3×3 Sobel edge response from the current frame with the squared difference between current and previous frames. Fusion avoids writing two intermediate images.\n\n(Image: Fused spatial and temporal video pipeline)","title":"Fused Sobel and motion detection"},{"category":"section","location":"applications/video/#See-it","page":"Sobel plus motion","text":"<!-- ATTENTION au chemin : Documenter NE RÉÉCRIT PAS les liens dans un bloc `@raw html`, et\n     `prettyurls` (actif en CI, donc sur le site publié) rend cette page à\n     applications/<nom>/index.html, soit un niveau plus profond qu'en local. Le `../../` ci-\n     dessous est donc correct EN PRODUCTION et cassé dans un `make.jl` lancé sans CI=true.\n     Pour prévisualiser les médias en local : CI=true julia --project=docs docs/make.jl -->\n<div class=\"interleave-media\">\n  <video autoplay loop muted playsinline width=\"100%\" poster=\"../../assets/media/video_pipeline_demo.webp\">\n    <source src=\"../../assets/media/video_pipeline_demo.mp4\" type=\"video/mp4\">\n    <img src=\"../../assets/media/video_pipeline_demo.webp\" alt=\"Fused Sobel and motion pipeline\" width=\"100%\">\n  </video>\n  <p class=\"interleave-caption\">\n    Fused Sobel edge response and temporal motion differencing, the kernel benchmarked below.\n  </p>\n</div>\n\nnote: Rendered by the C++ implementation\nUnlike the audio demo, this clip was not re-rendered by this package. It comes from Legolas++, the C++ project these ideas come from, which ships the same fused kernel. The Julia port computes the same result — the test suite checks it against a scalar oracle — but re-rendering the clip needs the original footage, which is not in this repository.It is kept because it shows what the kernel does, which no diagram conveys. It is not evidence about this implementation's output or speed; for that, see the measurements below.\n\nEach output pixel depends on a neighbourhood of input pixels, but output pixels do not depend on earlier output pixels. Like depthwise convolution, the contiguous spatial loop is open to ordinary compiler vectorization. The benchmark tests whether packing independent streams is still worthwhile.","title":"See it"},{"category":"section","location":"applications/video/#The-benchmark","page":"Sobel plus motion","text":"bench/video.jl processes 256 independent pairs of 128×128 frames. Its operation estimate is 23 floating-point operations per interior output pixel.\n\nconfiguration time estimated GFlop/s speedup vs scalar\nscalar reference 2.17 ms 43.1 1.00×\nP=1 2.19 ms 42.7 0.99×\nP=2 5.04 ms 18.5 0.43×\nP=4 2.49 ms 37.5 0.87×\nP=8 2.48 ms 37.7 0.88×\nP=16 2.43 ms 38.4 0.89×\nP=32 2.35 ms 39.7 0.92×\n\nNo sequential packed configuration beats the ordinary layout. Fusion is valuable, but DLI is not what creates that value.","title":"The benchmark"},{"category":"section","location":"applications/video/#With-explicit-task-parallelism","page":"Sobel plus motion","text":"configuration time estimated GFlop/s speedup vs scalar speedup vs threaded P=1\nP=1, 10 threads 0.64 ms 145.8 3.38× 1.00×\nP=2, 10 threads 1.15 ms 81.2 1.88× 0.56×\nP=4, 10 threads 0.63 ms 149.4 3.46× 1.03×\nP=8, 10 threads 0.67 ms 139.8 3.24× 0.96×\nP=16, 10 threads 0.52 ms 178.8 4.15× 1.23×\nP=32, 10 threads 0.50 ms 186.5 4.32× 1.28×\n\nIn this particular run P=32 became fastest only after threading, by 28% over threaded P=1. That is much smaller—and more sensitive to cache, scheduling, and machine load—than the 4.32× comparison with a serial reference suggests. The defensible default remains P=1; retune the combined threaded configuration for the target stream count and frame size.","title":"With explicit task parallelism"},{"category":"section","location":"applications/video/#Competing-approaches","page":"Sobel plus motion","text":"Plain Julia/LLVM is already effective for the spatial loop. Keeping a fused loop and a normal array is the lowest-complexity baseline.\nTullio is well suited to expressing the Sobel stencil in index notation. Their legality assumptions must still be checked when output aliases an input.\nImage-processing or neural-network libraries are preferable when their primitive matches the full pipeline, especially if they provide optimised CPU and GPU backends. Separate Sobel and difference calls may, however, materialise intermediates unless fusion is available.\nA custom CUDA.jl kernel can map pixels and streams to GPU threads and preserve fusion. It is attractive for device-resident video, but launch, transfer, and integration costs matter for small CPU-resident frames.\nThreads are a complement rather than a rival to SIMD. Here they provide most of the gain; the table isolates the smaller incremental effect of packing.\n\nThis case study demonstrates the project's stop rule: a tool that can decline its own transformation is safer than one that labels every emitted vector instruction a success.","title":"Competing approaches"},{"category":"section","location":"#Interleave.jl","page":"Why Interleave?","text":"<div class=\"interleave-hero\">\n  <h2>Keep the recurrence. Vectorize the population.</h2>\n  <p>\n    Interleave turns a batch of independent, intrinsically sequential problems into SIMD work.\n    You keep one readable kernel; changing the array type changes what one element means\n    to the CPU.\n  </p>\n</div>\n\nMany useful algorithms contain a hard dependency:\n\ny_i = f(x_i y_i-1 y_i-2 ldots)\n\nThe value at i cannot be computed before i-1. A compiler cannot safely execute those two iterations together. But if you have hundreds of independent filters, linear systems, trajectories, or grid lines, there is another axis of parallelism: compute the same i for several problems at once.\n\n(Image: Scalar recurrence chains compared with SIMD lockstep)\n\n<p class=\"interleave-caption\">\n  Interleave does not break a dependency. It changes the direction in which SIMD is applied.\n</p>","title":"Interleave.jl"},{"category":"section","location":"#Why-not-use-a-conventional-SoA-layout?","page":"Why Interleave?","text":"That is the natural first alternative, and it exposes the essential trade-off:\n\nphysical layout pleasant part price paid\njob-major / AoS one recurrence is contiguous and reads naturally values from different jobs are far apart, so SIMD across jobs needs gathers\nglobal SoA all jobs at position i are contiguous and an explicit inner job loop can vectorize the algorithm must be transposed into “time outside, jobs inside”; the formulation needs a batch-sized workspace and keeps one strided stream per array, so it costs 2.2x on a tridiagonal solve and 11.8x on a pentadiagonal one, where the stream count overwhelms the prefetchers (measured)\nDLI / blocked SoA one element is a packet of P jobs, while packet i-1 remains adjacent to packet i P becomes a layout choice that must be tuned\n\nDLI is often called an AoSoA layout: it applies SoA only inside a cache-sized group of problems. The group advances through the complete recurrence before the next group starts. This preserves the natural one-problem kernel, gives unit-stride packet loads, and bounds the distance between consecutive recurrence states.\n\nAnd the global-SoA rewrite is measured, not assumed. An experienced reader does not compare this to a naive loop; they say \"I would just write it SoA by hand\". Sometimes they are right — on a first-order recurrence, hand-written SoA beats Interleave by 2.5×. As soon as the recurrence carries more than one predecessor the ordering reverses and the gap widens, because the SoA form needs one strided stream per array and the memory system falls off a cliff once there are more than about a dozen. The sweeps are in What you would write instead.\n\nThere is also an argument the stopwatch does not capture. You want to get the algorithm right in scalar form first, and a transposed loop nest is neither a natural thing to write nor a natural thing to debug. Interleave keeps that scalar kernel as the only kernel: the one you developed is the one that ships, and there is no transposed twin to keep in step. Where hand-written SoA wins on the clock, it still costs you that.","title":"Why not use a conventional SoA layout?"},{"category":"section","location":"#Is-this-tool-for-your-kernel?","page":"Why Interleave?","text":"<div class=\"interleave-grid\">\n  <div class=\"interleave-card\">\n    <h3>A strong fit</h3>\n    <p>You have many independent, similarly shaped problems.</p>\n    <p>Each problem contains a recurrence, feedback loop, triangular sweep, or another\n       dependency that blocks ordinary loop vectorization.</p>\n  </div>\n  <div class=\"interleave-card\">\n    <h3>Usually the wrong fit</h3>\n    <p>Your inner loop is already contiguous and vectorized, problems interact with each\n       other, or control flow differs heavily from one problem to the next.</p>\n    <p>Use a normal array or choose <code>P = 1</code> instead.</p>\n  </div>\n</div>\n\nThose two boxes are a starting point, not a verdict, and a kernel can move between them. Adding a recursive row filter to the Sobel pipeline — the canonical bad fit, measured at 0.69× — turns it into a 3.6× win without touching the stencil.\n\nTwo questions place a kernel, and the second is a gradient rather than a gate:\n\nWhat throughput does your ordinary reference reach? A slow scalar reference reveals a recurrence bottleneck; an already-fast one has little SIMD work left to recover.\nHow many predecessors does the recurrence carry? One, and a hand-written SoA layout beats Interleave. Beyond that the gap opens and keeps opening: 2.2× on a tridiagonal solve, 8.4× on a sixteenth-order IIR, 11.8× on a pentadiagonal one.\n\nThe honest way to state the second, because it is what the sweeps actually show: Interleave does not get faster as the recurrence hardens — the alternatives get slower. Across the video sweep its time rises 1.8× while the reference's rises 9.1×. All of it is measured in What you would write instead.\n\n(Image: Reference throughput versus measured DLI speedup)\n\nThe graph reports measurements from one Apple M-series machine, not promises. Always measure your kernel on your target CPU.\n\nThe classical roofline model adds a useful second explanation. Packing does not change arithmetic intensity: it removes the lower dependency-latency ceiling that kept a scalar recurrence far below both the bandwidth and compute roofs. Once that ceiling is removed, Thomas eventually becomes bandwidth-bound; a cache-resident biquad can remain limited by arithmetic latency. Conversely, a stencil already close to a hardware roof has little room for DLI.\n\n(Image: A recurrence-aware interpretation of the roofline model)\n\nThis view follows the Legolas++ roofline analysis, but the points and ceilings must be remeasured for Julia and for each target CPU. A roofline is an upper bound, not evidence that a particular loop contains SIMD instructions.","title":"Is this tool for your kernel?"},{"category":"section","location":"#Install-the-experimental-package","page":"Why Interleave?","text":"Interleave is not registered yet. Install it directly from its repository:\n\npkg> add https://github.com/laurentplagne/Interleave.jl","title":"Install the experimental package"},{"category":"section","location":"#The-complete-idea-in-one-example","page":"Why Interleave?","text":"This causal low-pass filter is sequential along n:\n\nusing Interleave\n\nfunction lowpass!(y, x, α)\n    β = one(α) - α\n    @inbounds begin\n        y[1] = β * x[1]\n        for n in 2:length(y)\n            y[n] = α * y[n - 1] + β * x[n]\n        end\n    end\n    y\nend\nlowpass! (generic function with 1 method)\n\n@inbounds is not required for correctness or for using Interleave. It is a lexical promise made by the kernel author after testing: Julia cannot safely attach it later from apply!. For the Thomas kernel on the measured M1, retaining checks preserved SIMD but was about 30% slower. During development, remove the annotation; add it only when the index proof is clear.\n\nWrite and check it first with ordinary Julia arrays:\n\nfunction make_batch(Arr, nbatch, nsamples)\n    x = Arr([Float32(b) + Float32(n) / 32\n             for b in 1:nbatch, n in 1:nsamples])\n    y = Arr(zeros(Float32, nbatch, nsamples))\n    y, x\nend\n\nys, xs = make_batch(Base.Array{Float32,2}, 10, 16)\napply!((y, x) -> lowpass!(y, x, 0.8f0), ys, xs)\nys[1, 1:4]\n4-element Vector{Float32}:\n 0.20624998\n 0.3775\n 0.52075\n 0.6416\n\nThen change only the storage type:\n\nyv, xv = make_batch(Interleave.Array{Float32,2,4}, 10, 16)\napply!((y, x) -> lowpass!(y, x, 0.8f0), yv, xv)\n\n(ys == yv, eltype(parent(yv)), size(parent(yv)))\n(true, Vec{4, Float32}, (16, 3))\n\nThe kernel still sees a one-dimensional array. With the standard batch its elements are Float32; with the interleaved batch they are Vec{4,Float32}. Arithmetic on one element therefore becomes arithmetic on four independent filters.\n\n(Image: Scalar loads and four-lane packet loads through the low-pass recurrence)\n\n(Image: A common scalar layout and the DLI physical storage)\n\nThe logical array remains scalar: size(yv) == (10, 16) and yv[3, 7] is a Float32. Only parent(yv) exposes the packed representation used by the hot kernel.\n\nThe comprehension constructor is intentionally convenient, not zero-copy: Julia first materializes the scalar matrix, then Interleave packs it. When initialization belongs to the timed path or the batch is very large, allocate with undef and fill by broadcast, a loop, or a device initialization kernel. Measure packing as part of the end-to-end pipeline.","title":"The complete idea in one example"},{"category":"section","location":"#What-is-novel-here?","page":"Why Interleave?","text":"Interleave combines four ideas into one programming model:\n\nSIMD follows semantic independence, not necessarily the innermost loop. The packed axis is a population of problems rather than consecutive steps of one problem.\nThe element type is the optimization switch, and that is a correctness property. The same source kernel runs on a scalar element or a SIMD packet, so there is no second \"vector implementation\" to maintain — and no second implementation means nothing that can drift. The alternative is not hypothetical: writing the hand-rolled SoA variants used in the comparisons introduced a real bit-exactness bug, one Float32 ulp from an associativity slip in a boot loop that exists only because the layout changed. It looked like a property of SoA until it was tracked down. A second layout is a second source of truth, and second sources drift.\nThe logical and physical arrays disagree on purpose. Users read a scalar batch while kernels receive dense arrays of packets.\nThreading remains explicit. apply! is always sequential; parallel_apply! is visibly parallel at the call site.\n\nThe DLI idea comes from Legolas++, but Julia removes most of its C++ machinery: multiple dispatch, AbstractArray, views, and a concrete SIMD element type are enough.","title":"What is novel here?"},{"category":"section","location":"#Choose-a-path","page":"Why Interleave?","text":"If this is your first visit, start with the Thomas tutorial, then the filter bank, 3-D ADI, and performance decision tutorials. Together they move from the core mechanism to realistic layout and tuning decisions.","title":"Choose a path"},{"category":"section","location":"#Five-applications,-including-the-failures","page":"Why Interleave?","text":"The bench/ case studies apply the same decision process to every benchmark shipped with the project:\n\nrecurrence-heavy Thomas, IIR biquad, and Black–Scholes Crank–Nicolson kernels;\na depthwise convolution and a fused Sobel-motion pipeline that LLVM already vectorizes.\n\nYou can also hear the filter bank — the audio is produced by this repository's own kernel — and see the Sobel-motion pipeline.\n\nEach study includes an animated explanation, the complete P sweep, explicit threaded results, and a comparison with the most relevant alternatives. The negative cases are part of the design argument: Interleave provides P=1 so the same kernel can decline DLI when ordinary spatial SIMD is better.\n\nSee Positioning and alternatives for the wider comparison with compiler vectorization, Tullio, explicit SIMD, task schedulers, domain libraries, and GPU implementations.","title":"Five applications, including the failures"},{"category":"section","location":"tutorials/biquad/#Tutorial-2-—-A-bank-of-feedback-filters","page":"2 · Feedback filter bank","text":"An IIR biquad is a compact example of a temporal recurrence. Output sample n depends on earlier outputs, so SIMD across time is unsafe. Audio channels, sensors, simulation members, or parameter sets are often independent, however. They make a natural packed axis.\n\n(Image: Independent biquad states advancing in SIMD lockstep)","title":"Tutorial 2 — A bank of feedback filters"},{"category":"section","location":"tutorials/biquad/#1.-Write-one-filter","page":"2 · Feedback filter bank","text":"The kernel keeps four state values. When eltype(y) is a SIMD packet, every state variable becomes one packet too—one state per channel and no state sharing between lanes.\n\nusing Interleave\n\nfunction biquad!(y, x, coeffs)\n    b0, b1, b2, a1, a2 = coeffs\n    z = zero(eltype(y))\n    x1 = z; x2 = z; y1 = z; y2 = z\n\n    @inbounds for n in eachindex(y)\n        x0 = x[n]\n        y0 = b0*x0 + b1*x1 + b2*x2 - a1*y1 - a2*y2\n        y[n] = y0\n        x2 = x1; x1 = x0\n        y2 = y1; y1 = y0\n    end\n    y\nend\n\ncoeffs = Float32.((0.2, 0.4, 0.2, -0.3, 0.1))\n(0.2f0, 0.4f0, 0.2f0, -0.3f0, 0.1f0)\n\nNotice what is absent: the channel number. The driver owns the batch traversal; the kernel owns one causal signal.","title":"1. Write one filter"},{"category":"section","location":"tutorials/biquad/#2.-Create-distinct-channels","page":"2 · Feedback filter bank","text":"Distinct inputs matter in tests. If every lane contains the same values, a lane permutation bug can remain invisible.\n\nfunction signals(Arr, nchannels, nsamples)\n    x = Arr([(1f0 + Float32(channel) / 100) * cospi(Float32(n) / 32)\n             for channel in 1:nchannels, n in 1:nsamples])\n    y = Arr([0f0 for _channel in 1:nchannels, _n in 1:nsamples])\n    y, x\nend\n\nys, xs = signals(Base.Array{Float32,2}, 61, 256)\nyv, xv = signals(Interleave.Array{Float32,2,8}, 61, 256)\n\napply!((y, x) -> biquad!(y, x, coeffs), ys, xs)\napply!((y, x) -> biquad!(y, x, coeffs), yv, xv)\nys == yv\ntrue","title":"2. Create distinct channels"},{"category":"section","location":"tutorials/biquad/#3.-Recognize-the-application-pattern","page":"2 · Feedback filter bank","text":"This layout works well for:\n\nmany audio channels using the same filter structure;\nmany sensors sampled on the same clock;\nan ensemble evaluated with different inputs or parameters;\na cascade in which every lane executes the same number of stages.\n\nIt becomes less attractive when channels have different lengths, switch algorithms often, or require lane-dependent early exits. A SIMD packet progresses at the pace of its slowest lane.","title":"3. Recognize the application pattern"},{"category":"section","location":"tutorials/biquad/#4.-State,-coefficients,-and-parameters","page":"2 · Feedback filter bank","text":"Values stored in the arrays become lane-varying automatically. Plain scalar arguments are shared by all lanes. This distinction is useful:\n\nstore a coefficient in a Interleave array when it differs by channel;\npass it as a scalar or tuple when it is common to the entire batch;\nuse lanetype(eltype(y)) to construct a scalar literal of the right precision.\n\nFor example, a normalized constant that works for scalar and packed elements is:\n\none_eighth(y) = lanetype(eltype(y))(0.125)\n(one_eighth(packet(ys, 1)), one_eighth(packet(yv, 1)))\n(0.125f0, 0.125f0)","title":"4. State, coefficients, and parameters"},{"category":"section","location":"tutorials/biquad/#5.-What-the-speedup-means","page":"2 · Feedback filter bank","text":"On the measured workload, the ordinary biquad reference delivered about 2.9 GFlop/s and the best sequential DLI configuration reached a 15.19× speedup. This is precisely the useful regime: the temporal dependency leaves SIMD units underused, while independent channels supply abundant lane-level work.\n\nThat result does not transfer automatically to a short buffer, a different coefficient form, or a different CPU. Measure setup and conversion costs as part of the real pipeline. The biquad application study reports the complete sequential and threaded packet-size sweeps and compares domain-specific alternatives.","title":"5. What the speedup means"}]
}
